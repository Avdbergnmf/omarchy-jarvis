Working directory: use Alex’s assigned Jarvis worktree; stay there if already isolated.
Another agent may already own an in_progress assignment. You take a PARALLEL track only.

1) Run `./scripts/assignment-status.sh`. Read START.md parallel rules, QUEUE.md, SESSION.md.
2) Note every `in_progress` row's `area:` (and any optional path hints in its active/*.md — context only).
3) Claim the first `queued` assignment with **parallel-ok: YES** whose `area:` differs from every in_progress row's area. Path hints never override this — same area, no claim, even if paths look disjoint.
4) If none: stop with the same **“No assignment in queue is possible right now.”** + queue list as in NEW_AGENT.txt. Do not touch in_progress work.
5) If claimed: mark in_progress, update SESSION “Parallel agent”, work within the assignment's area/goal/out-of-scope (any path hints are non-binding), finish → done/, then **land on main** before stopping.

## Required worktree setup (before claim edits or coding)
Concurrent agents must each use a separate worktree + branch, even if clean.
If already assigned an isolated directory, verify it and resume there; do not
run the creation commands again. Otherwise, after read-only claim checks:

    git worktree list
    git status --short --branch
    git fetch origin
    jarvis_task=a014-example  # replace with your assignment's unique branch/slug
    jarvis_tree="$HOME/Work/omarchy-jarvis-$jarvis_task"
    git worktree add -b "$jarvis_task" "$jarvis_tree" origin/main
    cd "$jarvis_tree"
    git status --short --branch

Use START's isolation section for offline/existing-branch handling and cleanup.
Never checkout/stash/reset/clean/edit another agent's tree. Before claiming,
check the listed trees' QUEUE/SESSION + scopes read-only and coordinate with the
desk: each is a branch snapshot, not a global lock. Record owner, branch,
absolute worktree path and Batch in your own SESSION; reconcile shared status
files on merge without reverting others' claims. Isolation does not waive
parallel-ok/area rules (path hints stay optional, never a gate) or permit shared-service restarts.
Commit the scoped assignment, then `git push -u origin HEAD`.

## Land on main before you stop (required)
Leaving finished work only on a feature branch causes forgotten merges, conflicts, and wasted tokens.

After your batch is accepted (default: one assignment):
1. Ensure QUEUE/INDEX/SESSION/PROGRESS for this work are correct on **this branch**, then push the branch with `git push -u origin HEAD`.
2. **Merge into `main` and push `main`** before reporting done:
   - Preferred: create a PR with `gh pr create --title "..." --body "..."` if needed, then `gh pr merge --merge` (or squash if that is the repo norm), then `git fetch origin && git checkout main && git pull`.
   - If PRs are not required / you have push access: merge your branch into local `main`, resolve bookkeeping conflicts without wiping other claims, then `git push origin main`.
3. Reconcile shared bookkeeping on **main** so the next CONTINUE sees `done` / released claims (same spirit as cross-worktree claim visibility).
4. Only then remove your worktree/branch per START cleanup rules.
5. Your final report to Alex must include: merged yes/no, PR URL if any, and confirmation that `origin/main` has the update.

If merge is blocked (permissions, failed checks, conflicts you cannot safely resolve): open/update the PR, say **BLOCKED ON MERGE**, and do not claim you finished landing.

After merge to main is done, from a remaining checkout (actual path/branch):

    git -C "$HOME/Work/omarchy-jarvis-a014-example" status --short
    git worktree remove "$HOME/Work/omarchy-jarvis-a014-example"
    git branch -d a014-example

Remove only clean, merged, unowned trees; never force. Default batch size is 1:
stop and summarize after this assignment unless Alex explicitly enabled more.
