cd ~/Work/omarchy-jarvis
You are continuing Omarchy Jarvis work. Do NOT ask for chat/session logs.

1) Run `./scripts/assignment-status.sh`. Read START.md (skim), QUEUE.md, SESSION.md.
2) Resume the assignment marked **in_progress** that SESSION points at (or the one the user named). Read `docs/assignments/active/…`.
3) Do the next unchecked checklist item / SESSION “Next action”.
4) Update checklist + SESSION as you go; before stopping, flush Next action.
5) When acceptance is met: QUEUE/INDEX → done, move file to docs/assignments/done/, PROGRESS note, stub SESSION.
6) If user said “next step” and current is already done: run the **claimable** logic from NEW_AGENT.txt (parallel-aware). If nothing claimable → “No assignment in queue is possible…” + queue list (or empty).
7) Do not start conflicting parallel work unless the user said parallel / NEW_AGENT and claim rules allow it.
