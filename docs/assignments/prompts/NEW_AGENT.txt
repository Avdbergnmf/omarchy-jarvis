Working directory: use Alex’s assigned Jarvis worktree; stay there if already isolated.
You are a NEW agent on Omarchy Jarvis (cold start). No prior chat context.

## Where “who is working” lives
- **`origin/main`'s `docs/assignments/QUEUE.md`** is the only canonical claim truth (A-039) — a branch-local edit does not count until pushed there.
- **docs/SESSION.md** — which assignment the current session claims + Next action + optional Batch
- Run **`./scripts/assignment-status.sh`** first — it fetches and reads `origin/main`, not your local branch

## Batch policy (important)
- **Default:** do **exactly one** assignment, then **stop and report** to Alex.
- **Keep going** only if Alex explicitly asked (`keep going`, `batch N`, `do N assignments`, `until queue empty`). Record `Batch:` in SESSION. Still stop early if tokens are tight or nothing claimable.

## Mandatory order
1) Read START.md (job modes + parallel rules).
2) Skim docs/assignments/README.md.
3) Run `./scripts/assignment-status.sh`; read SESSION.md + QUEUE.md.

## Claiming work
A) If Alex named an assignment id → only if free or they said take over; else report conflict.
B) Else compute **claimable** queued rows:
   - `status=queued`
   - If any `in_progress`: only `parallel-ok: YES` with an `area:` different from every in_progress row's area; never claim `parallel-ok: NO` while something else is in_progress; never same area as an in_progress, even if that row's active/*.md lists path hints — paths are optional context, never a gate that excuses a same-area claim.
   - If nothing in_progress → first queued row.
C) Claimed → mark in_progress in QUEUE+INDEX, fill SESSION, **commit and push straight to `origin/main`** (see "Required worktree setup" below) *before* opening your worktree or writing product code, then execute.
D) If nothing claimable, do not code. Reply:

```
No assignment in queue is possible right now.

Queue:
- … (mark in_progress)
SESSION active: …
```

Or: `No assignment in queue is possible right now. The queue is empty.`

## After finishing one assignment
- Default: merge to main, then stop; tell Alex what done + merge/PR + next QUEUE row.
- If Batch allows more: claim next claimable and repeat until batch exhausted / nothing claimable / must stop.

## Do not
- Paste or demand prior agent chat logs
- Steal files from a non-parallel in_progress assignment
- Chain multiple assignments without an explicit batch/keep-going ask

## Push the claim to origin/main BEFORE opening a worktree (required, A-039)
`origin/main` is the only place a claim counts. From your current checkout:

    git fetch origin
    ./scripts/assignment-status.sh   # re-check: still claimable on origin/main?
    git add docs/assignments/QUEUE.md docs/assignments/INDEX.md docs/SESSION.md
    git commit -m "docs: claim A-NNN on main before opening worktree"
    git push origin main

If the push is rejected, `git fetch && git pull --ff-only`, re-check, and either
retry or pick a different claimable id — never force-push over another claim.

## Required worktree setup (only after the claim commit is on origin/main)
Concurrent agents must each use a separate worktree + branch, even if clean.
If already assigned an isolated directory, verify it and resume there; do not
run the creation commands again. Otherwise:

    git worktree list
    git status --short --branch
    git fetch origin
    jarvis_task=a014-example  # replace with your assignment's unique branch/slug
    jarvis_tree="$HOME/Work/omarchy-jarvis-$jarvis_task"
    git worktree add -b "$jarvis_task" "$jarvis_tree" origin/main
    cd "$jarvis_tree"
    git status --short --branch

Use START's isolation section for offline/existing-branch handling, recovering
a stale claim, and cleanup. Never checkout/stash/reset/clean/edit another
agent's tree. Before claiming, check the listed trees' QUEUE/SESSION + scopes
read-only and coordinate with the desk: a worktree's own copy can still lag
`origin/main`, which is why the claim above must land there directly, not on a
branch. Reconcile shared status files on merge without reverting others'
claims. Isolation does not waive parallel-ok/area rules (path hints stay
optional, never a gate) or permit shared-service restarts. Commit the scoped
assignment, then `git push -u origin HEAD`.

## Land on main before you stop (required)
Leaving finished work only on a feature branch causes forgotten merges, conflicts, and wasted tokens.

After your batch is accepted (default: one assignment):
1. Ensure QUEUE/INDEX/SESSION/PROGRESS for this work are correct on **this branch**, then push the branch with `git push -u origin HEAD`.
2. **Merge into `main` and push `main`** before reporting done:
   - Preferred: create a PR with `gh pr create --title "..." --body "..."` if needed, then `gh pr merge --merge` (or squash if that is the repo norm), then `git fetch origin && git checkout main && git pull`.
   - If PRs are not required / you have push access: merge your branch into local `main`, resolve bookkeeping conflicts without wiping other claims, then `git push origin main`.
3. Reconcile shared bookkeeping on **main** so the next CONTINUE sees `done` / released claims (same spirit as cross-worktree claim visibility).
4. Only then remove your worktree/branch per START cleanup rules.
5. Your final report to Alex must include: merged yes/no, PR URL if any, and confirmation that `origin/main` has the update.

If merge is blocked (permissions, failed checks, conflicts you cannot safely resolve): open/update the PR, say **BLOCKED ON MERGE**, and do not claim you finished landing.

## After merge: remove worktree and return to main (required)
Before you stop **or** claim another assignment / take a new CONTINUE prompt:
1. Confirm your work is on `origin/main` (merged).
2. From a remaining checkout (usually the canonical tree), remove **your** finished worktree and delete the merged branch (never force; never touch another agent's tree):

```bash
git -C "$HOME/Work/omarchy-jarvis-<your-slug>" status --short
git worktree remove "$HOME/Work/omarchy-jarvis-<your-slug>"
git branch -d <your-branch>
```

3. `cd` back to the canonical checkout and update main:

```bash
cd "$HOME/Work/omarchy-jarvis"
git checkout main
git pull origin main
```

4. Only then stop and report — or, if Batch allows another assignment, claim the next one **from this updated `main`** (new worktree for the new assignment). Do not start the next prompt while still sitting in the old feature worktree.

Remove only clean, merged, unowned trees; never force. Default batch size is 1 unless Alex explicitly enabled more.
