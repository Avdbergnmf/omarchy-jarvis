# Decisions log

Format: **ADR-NNN — Title** (date). Context → Decision → Consequences.

## ADR-001 — Ollama over LM Studio (2026-09-09)
- **Context:** Need a local lightweight tool-calling brain; LM Studio felt like extra GUI fluff.
- **Decision:** Use **Ollama** CLI/API as the only local runtime for v0.
- **Consequences:** Simpler headless ops; pull a small instruct+tools model (≤3B preferred on this VM historically; RAM is now ~15 GiB so 3B–7B is plausible — start 3B, document).

## ADR-002 — Overlay = local web UI in Hyprland float (2026-09-09)
- **Context:** Need something easy to ship and maintain as the *main* input surface.
- **Decision:** Small localhost web UI (HTML/JS or minimal Vite) shown as a centered floating Hyprland window with windowrules; not GTK-first for v0.
- **Consequences:** Fast UI iteration; must add solid `windowrulev2` and a single-instance toggle. Can later swap shell without rewriting tools.

## ADR-003 — Keybinds as data + composed skills (2026-09-09)
- **Context:** Fear of rewriting rules when expanding beyond workspaces.
- **Decision:** Ship `run_binding` fed by `omarchy menu keybindings --print` from day one, plus a few composed tools/skills (`scratch_toggle`, `open_planning`).
- **Consequences:** Model never owns chord strings; catalog stays in sync with Omarchy updates.

## ADR-004 — Confirm before saving skills (2026-09-09)
- **Context:** Cloud/agents may draft automation; user wants control.
- **Decision:** Skill drafts land in `skills/drafts/`; promote to `skills/` only after explicit user OK.
- **Consequences:** UI/CLI must support propose → show diff → confirm.

## ADR-005 — Thought bubbles → live console (2026-09-09)
- **Context:** Debugging agent thoughts; file-only logs are weak.
- **Decision:** Use `omarchy-notification-send` with `--exec` launching a **live** console (tail -F / streaming) for that run id.
- **Consequences:** Each run gets `logs/runs/<id>.log`; console attaches to the stream.

## ADR-006 — Google Calendar vs stock “Calendar” binding (2026-09-09)
- **Context:** User’s planning stack includes Google Calendar. Stock **SUPER + SHIFT + C** opens HEY calendar; **SUPER + CTRL + ALT + D** toggles Omarchy clock shell, not Google Calendar.
- **Decision:** Planning skill opens Google Calendar via `omarchy-launch-or-focus-webapp` / `omarchy-launch-webapp https://calendar.google.com/` — do not call the HEY binding for “planning”.
- **Consequences:** Document clearly so agents don’t “fix” Calendar by simulating SUPER+SHIFT+C.

## ADR-007 — Host Lua dispatch and reviewed binding map (2026-09-09)
- **Context:** Live Hyprland 0.56 rejects legacy `hyprctl dispatch workspace ...`; `binds` contains opaque `__lua` callbacks, and catalog output has no command column.
- **Decision:** Use the installed Omarchy Lua expressions (`hl.dsp.focus`, `hl.dsp.window.move`, `hl.dsp.workspace.toggle_special`). Refresh the catalog before resolving an exact chord/description; execute reviewed scratch, Outlook, WhatsApp and numbered-workspace mappings only.
- **Consequences:** Unsupported bindings fail explicitly. Never execute arbitrary Lua callback IDs or model-generated shell. Syntax references: installed `/usr/share/omarchy/default/hypr/bindings/tiling.lua` and https://wiki.hypr.land/Configuring/Basics/Window-Rules/.

## ADR-008 — Workspace placement and launcher safety (2026-09-09)
- **Context:** Focus helpers can jump back to another workspace; the packaged focus wrapper constructs a shell command with eval. The current default browser is Brave despite Chromium being present.
- **Decision:** Reuse a matching app window or launch via the argv-safe `omarchy-launch-webapp`; move each verified window address to the lowest unused positive numbered workspace for planning. Read literal personal Outlook/WhatsApp URLs from bindings.lua.
- **Consequences:** Planning consolidates existing app windows. App identity and final workspace are checked; timeouts return partial progress. No new browser profile is used for the account webapps.

## ADR-009 — Strict JSON plans after failed native tool-call demos (2026-09-09)
- **Context:** qwen2.5:3b native tool calling created only a workspace for the planning request and falsely claimed completion. It also composed scratch_move_here with the full scratch-and-mail recipe, duplicating a mutation.
- **Decision:** Default to Ollama `/api/chat` structured output with a strict action-plan schema and concrete recipe examples. Validate the entire plan before executing any action, reject duplicate calls and recipe/action mixtures, and derive completion messages from successful tool results. Keep the bounded native tools loop opt-in (`JARVIS_PLANNER=tools`) for future model evaluation.
- **Consequences:** No regex prompt router or cloud runtime. Plans are local model output, executed through the same reviewed argv CLIs. End-to-end evidence must prove a new workspace and actual full recipe invocation, not trust model replies. See https://docs.ollama.com/capabilities/tool-calling and https://docs.ollama.com/capabilities/structured-outputs.

## ADR-010 — Local HTTP protection and focus capture (2026-09-09)
- **Context:** A localhost action API must not accept requests from unrelated web pages. The overlay steals focus from the user's intended scratchpad target.
- **Decision:** Bind only 127.0.0.1:7421; require a per-process token, exact Host and same Origin for mutations, no CORS, bounded request sizes, one active desktop run. Record the target address before opening the overlay, close the overlay before actions, and restore that target or fail if it disappeared.
- **Consequences:** The isolated Chromium overlay profile is runtime-only under ignored logs; account webapps keep the user's default browser profile. Escape and toggling close only the Jarvis overlay. Notifications carry action summaries/results, never private model reasoning.

## ADR-011 — One typed action per v0 request (2026-09-09)
- **Context:** An unconstrained JSON actions list still made qwen2.5:3b combine a scratch action with its full recipe. A workspace test also emitted a string instead of an integer.
- **Decision:** Constrain structured generation with per-tool argument schemas and at most one action; compound workflows use a complete approved skill. Invalid plans fail before any desktop mutation.
- **Consequences:** Both recipe variants and planning pass model evaluation. Arbitrary multi-action composition is deferred; extend by adding reviewed recipes. Completion text comes from successful actions rather than model claims.

## ADR-012 — M4 CLI stub and explicit approval (2026-09-09)
- **Context:** M0–M3 includes a usable but limited confirm-before-save path.
- **Decision:** A local CLI copies candidates into drafts, shows complete file diffs, and promotes only an explicitly approved SHA-256 digest. Changes invalidate that digest; existing installations cannot be overwritten. Promoted scripts remain CLI-only.
- **Consequences:** The two requested example skills are preapproved by the implementation request. Future recipes require user approval; no model tool can promote drafts. M4 UI/discovery remains open in issue #5.

## ADR-013 — Chromium app identity on repeat launches (2026-09-09)
- **Context:** `--class=jarvis-overlay` was honored on first launch, but Chromium changed subsequent app windows to a URL-derived class even after restarting its dedicated profile. Tests caught tiled duplicate windows.
- **Decision:** Serve the overlay at the unique `/jarvis-overlay` route. Match both the requested class and Chromium's observed `chrome-127.0.0.1__jarvis-overlay-Default` in window rules, focus tracking and single-instance checks. Keep the dedicated profile and disable extensions; do not kill browser processes as part of normal toggling.
- **Consequences:** Actual browser class differs from the handoff's suggested literal on repeat launches. Matching the stable app identity preserves floating, centering, Escape, focus restoration and single-instance behavior without depending on Chromium honoring a flag.

## ADR-014 — Approve-before-execute transparency pass (2026-09-09)
- **Context:** v0 executed tools immediately after planning; the overlay only showed "Thinking…" then a final reply, so the user could not see or stop a mutating action before it ran. User feedback asked for **see + control**: visible plan/tool IO and no desktop mutation without explicit Run.
- **Decision:** Every prompt now runs in two phases. `POST /v1/run` only plans (JSON planner, or the first tool-call turn under the opt-in `JARVIS_PLANNER=tools` loop) and returns `status=awaiting_approval` with the proposed `plan` when it has actions; chitchat with zero actions still finishes immediately with `status=done` (no approval step). `POST /v1/runs/<id>/approve` executes the stored plan; `POST /v1/runs/<id>/deny` cancels with no side effects. `GET /v1/runs/<id>` now returns `status | plan | steps[] | reply` so the overlay can render a live per-tool step list during execution. A run held `awaiting_approval` for more than `AWAIT_TIMEOUT` (900s) auto-denies and releases the single-run lock so an abandoned overlay can't wedge the brain. Approve/deny is per-run, not per-step (`approve_step` from the design doc is deferred — the JSON planner already caps a plan at one action/one complete recipe, so run-level approval already gives step-level granularity in practice; add `approve_step` only if plans grow beyond one action).
- **Consequences:** `execute_json_run`/`execute_run` were replaced by `plan_and_maybe_run`/`execute_plan` (JSON planner) and `plan_tools_run`/`execute_tools_plan` (opt-in tools loop), all gated by the same approve/deny/expiry code path so the experimental loop can't bypass control. The overlay grew a plan checklist, live step list, Run/Cancel buttons, and an always-visible **Open console** button (see ADR-015). `scripts/demo-test.py` now approves/denies through the API instead of expecting immediate execution.

## ADR-015 — Config file location, notification/approval modes, and overlay size (2026-09-09)
- **Context:** The design doc asked for a config file (approval_mode, show_notifications) without a rebuild, and for the overlay to grow enough to show the plan/step UI plus an always-available console button.
- **Decision:** Config lives at `~/.config/jarvis/config.toml` (same convention as `~/.config/hypr/bindings.lua` — a per-user Omarchy dotfile, not repo-tracked), read once at server start via stdlib `tomllib`. Missing file or invalid `approval_mode` silently falls back to the safe default `"always"`. `approval_mode = "skills_trusted"` auto-runs a plan only when every action is `run_skill` (a reviewed recipe); anything else still awaits approval. `approval_mode = "off"` auto-runs everything, logs a warning line to the run log, and the overlay footer shows "⚠ approval off". `show_notifications = false` keeps writing the run log but skips the desktop notification. The overlay window grew from 520×150 to **640×460** (`scripts/toggle-overlay.py` `--window-size`, the `o.window(...)` rule in `scripts/install-hotkey.py`, and the matching assertion in `scripts/verify-host.py`) to fit the plan checklist and step list. The **Open console** button is always rendered (never hidden), disabled only until a run id is known; the current run's id is written to the overlay's own `localStorage` as soon as `/v1/run` returns, so the button stays enabled — pointing at the current or most recent run — even across closing and reopening the overlay window (a fresh page load each time). Escape both denies any run still `awaiting_approval` and closes the overlay, so it can never leave an orphaned pending run.
- **Consequences:** New endpoint `POST /v1/runs/<id>/console` spawns `console/jarvis-console <id>` server-side (same validated argv pattern as the existing notification `--exec`), since browser JS cannot exec a process itself. `GET /health` now also reports `ollama: ok|fail` and `approval_mode` for the overlay footer. Verified live on this host: plan/approve/execute, deny-then-nothing-happened, and Open console re-enabling on overlay reopen after a service restart (see PROGRESS.md).

## ADR-016 — Self-improve loop: deterministic intake, not model-driven drafting (2026-09-09)
- **Context:** `docs/passes/active/CLAUDE_SELF_IMPROVE_PASS.md` asked for Jarvis to file rich bug/feature records with clarifying Q&A, a local backlog, and prepared (never auto-sent) handoffs to other agents — built on top of the plan → approve → execute control plane from ADR-014/015. The obvious approach would let the local Ollama model draft the issue title/body/questions itself, but ADR-009 already found qwen2.5:3b unreliable at freeform structured output (incomplete plans, false completion claims) even with a bounded single-action schema.
- **Decision:** Bug/feature intake is entirely deterministic, not model-driven. `POST /v1/run` detects intent via regex — `/report`/`/feature` slash prefixes, or natural-language triggers ("you messed up", "I wish it could…") — and `/backlog`, `/dispatch <issue> to <agent>`, entirely **outside** the JSON/tool-calling planner. A fixed 3-question script per kind (bug: expected/actual/repro; feature: what/why/constraints) is asked one at a time via new `status=awaiting_answer` run state and `POST /v1/runs/<id>/answer` (reply `"skip"` to stop early). The server itself gathers the context pack (last run's plan/approval/log tail; a live `hyprctl clients/workspaces` snapshot and `catalog_bindings` query, each only when the seed text looks window/binding-related, to avoid dumping irrelevant desktop state into every issue) and fills `docs/templates/ISSUE_BUG.md` / `ISSUE_FEATURE.md`. `report_bug`/`report_feature` are excluded from both the JSON planner's `PLAN_SCHEMA` and the tool-calling loop's tool list (`LLM_EXCLUDED_TOOLS`) — the model never sees them, so it can't hallucinate a filed issue's content. The finished draft becomes an ordinary one-action plan (`report_bug`/`report_feature`) and goes through the **same** approve/deny/expiry path as every other tool call (reusing a new `commit_plan` helper factored out of `plan_and_maybe_run`); the overlay shows the full title+body as a `draft` preview before Run. `report_bug`/`report_feature`/`list_backlog`/`prepare_handoff` are also exempted from `execute_plan`'s `restore_target` (no desktop window to restore for filing an issue or writing a local file). Redaction (`core.redact`, extended with `sk-`/`gh[ops]_` token patterns) is applied both when the server assembles the draft and again inside the `report_bug`/`report_feature`/`prepare_handoff` actions themselves, so direct CLI/skill use (e.g. `report-last-failure`) is never unredacted just because it skipped the server's intake path.
- **Consequences:** Filing is slower to build (a fixed script, not a smart assistant) but predictable and testable without a live model call — `SelfImproveServerTest`/`BacklogActionsTest` cover intent detection, the Q&A state machine, redaction, and template filling with mocks only. `gh issue create --label` fails outright if a label doesn't exist yet in the repo; `report_record` now calls `gh label create --force` (idempotent create-or-update) for its four labels (`bug`, `jarvis-reported`, `enhancement`, `backlog`) immediately before creating the real issue — discovered live on this host on the first real filing attempt (see PROGRESS.md). Dispatch is prepare-only by design: `prepare_handoff` writes `docs/backlog/handoffs/issue-<n>-<agent>.md` and never calls any agent API — no real-send hook exists in this pass at all, for any agent (a non-goal per the source doc); `docs/backlog/README.md` documents `dispatch.enabled = false`-by-default as the convention a future hook would have to follow, not a key the code reads today.

## ADR-017 — Agent front door, scope and disposable briefs (2026-09-09)
- **Context:** Multiple historical passes and prompts (`ASTRA_HANDOFF`, `CLAUDE_*_PASS`, `PROMPT_*`) made it ambiguous which doc a cold agent should read first, or which brief was still current versus superseded. There was also no shared convention for which files two agents could touch in parallel without colliding.
- **Decision:** `START.md` is the front door — first lines say to read it fully before any other doc or code — and owns job modes (fix issues / implement a pass / feature from backlog / explore-debug) and the Issue Loop; `AGENTS.md` is slimmed to durable invariants only, pointing at `START.md`. Passes and handoffs each get `{README,INDEX,active/,archive/}` with INDEX status `active | done | superseded`; a brief moves to `archive/` on merge/close and is never treated as current instructions once archived. Parallel work is scoped by `area:{overlay,brain,actions,skills,docs}` labels; `parallel-ok` requires explicit comma-separated `Allowed paths:`/`Forbidden paths:` in the issue body, `single-writer` is the default for `area:brain`/control-plane work, and `scripts/agent-status.py` (`ensure_labels`-style idempotent GH label list — `area:*`, `parallel-ok`, `single-writer` — created on this repo) parses those path lists to warn on overlapping scope or shared areas between open issues.
- **Consequences:** Existing `docs/ASTRA_HANDOFF.md`, `docs/CLAUDE_*_PASS.md` and `docs/PROMPT_*` files migrated into `docs/passes/{active,archive}/` with an `INDEX.md`; every cross-reference to their old paths (ADR-016, `docs/PROGRESS.md`) was updated rather than left dangling. `agent-status.py`'s collision/scope warnings are conservative prefix-overlap heuristics for human review, not an enforcement mechanism — they never block or auto-schedule anything, and area/parallel-ok labels coordinate but do not authorize spending or spawning other agents (unchanged from ADR-016's dispatch-is-prepare-only stance). Covered by `tests/test_journal.py::JournalTest.test_status_scope_collisions`.

## ADR-018 — Versioned run journal, debug-only module internals (2026-09-09)
- **Context:** `logs/runs/<run_id>.log` (ADR-005) was coarse free-text — useful for a live `tail -F` console but not a structured record an agent could `jq` for "what did the user ask, what did Jarvis decide, what actually happened, does anything look wrong." Per-tool argv/stdout spam at the same verbosity as user-facing events also made the log noisy for a human skimming it.
- **Decision:** `brain/journal.py` is the primary evidence: an append-only JSONL journal at `logs/journal/CURRENT.jsonl`, four bounded records per terminal run — `prompt → process → done → eval` — each carrying `ts`, `jarvis_version` (from `VERSION`), and `git_describe` (cached once at process startup, not shelled out per-run or per-poll). `evaluate()` is a local, synchronous, rule-only check (approved plan vs. executed steps; no subprocess, model, or desktop snapshot) that sets `flag: null|suspicious|mismatch|partial` — conservatively `suspicious` even on a fully successful run, since matching tool return codes can't independently prove the desktop actually changed. Each run's journal records are also appended to `logs/runs/<run_id>.log` unchanged, so the existing `jarvis-console`/notification `--exec` path keeps working against the same file, just now JSONL instead of prose. Module/internal detail (raw argv, poll noise) is `debug`-only via `log_level = "debug"` in `~/.config/jarvis/config.toml` (default `"info"`), written to `logs/debug/<run_id>.jsonl` instead of the journal. A `VERSION` file plus "change VERSION → restart → first journal write rotates `CURRENT.jsonl` to `logs/journal/archive/v<old>-<UTC timestamp>.jsonl`" is the version-reset procedure, so evidence from a since-changed code path stays attributable to the commit that produced it via `git_describe`.
- **Consequences:** No cloud LLM call and no extra subprocess/hyprctl snapshot on the eval path — flags are cheap heuristics for a human or agent to triage, never automatic GitHub issues (bug intake can still pull the flagged excerpt in if the user `/report`s). `GET /v1/runs` polling stays silent (no journal/debug write per poll). Journal/debug directories are gitignored, mode 0700/0600; code and schema (this ADR, `docs/LOGGING.md`) are the durable git-tracked record, not the runtime data itself. Verified live: a real "say hello" run produced the four phases with real `jarvis_version`/`git_describe` (the latter correctly `-dirty`-suffixed on an uncommitted tree), and a real approved `list_backlog` run's eval correctly flagged `suspicious` per the conservative-on-success rule (see PROGRESS.md). Covered by `tests/test_journal.py` (rotation, redaction/bounds, all four phases across six run outcomes, write-failure still releases the busy lock, debug opt-in).

## ADR-019 — Overlay stays open through execution instead of auto-closing on Run (2026-09-09)
- **Context:** ADR-010 closed the overlay window as part of `restore_target()`, called at the very *start* of `execute_plan`/`execute_tools_plan` — before any step ran or the overlay's own poller could render a single "Running …" line. The overlay's live step-list UI (ADR-014) was therefore effectively unreachable: pressing Run destroyed the window showing it within milliseconds. Worse, `restore_target()` then re-focused the *pre-overlay* window, which each action's own dispatch (`open_app`, workspace switches, `run_skill`) immediately re-focused past on completion — for a newly-launched webapp that can take up to ~30s (`open_app`'s launch-and-poll loop). Net effect, matching Alex's report: press Run, the overlay vanishes, the screen flashes to the old window, and the real result appears later with nothing visible signaling progress in between — "a flash then back to nowhere," not something that "feels permanent."
- **Decision:** `restore_target()` keeps focusing the pre-overlay target before an action runs (still required so `scratch_move_here`/current-window-dependent `run_binding` chords operate on the window the user meant, not the overlay) but no longer closes any window. The overlay now stays open for the whole run — planning, awaiting approval, executing with a live step list, and the terminal `done`/`error` reply — and is dismissed only by the user: **Esc** (`POST /v1/close`, unchanged) or pressing the hotkey again, which reuses `toggle-overlay.py`'s existing single-instance logic (close if the overlay is the focused window, otherwise just refocus it and refresh `overlay-target.json` to whatever's active now). Chitchat and denied plans were never affected either way — `commit_plan`'s zero-action branch and `handle_deny` never called `restore_target` in the first place.
- **Consequences:** The user can now actually watch a run's step list happen instead of losing the window an instant after pressing Run; a workspace switch or new app launch is visible around/behind the (640×460, not fullscreen) overlay rather than hidden behind an premature refocus. No new endpoint or client-side auto-close was added — deliberately minimal, since `toggle-overlay.py` already had correct single-instance open/close/refocus behavior that just wasn't being exercised as the run's dismissal path. If the overlay ever needs to auto-dismiss after a delay, that's a separate follow-up, not required by this fix. Covered by `tests/test_jarvis.py::BrainTest.test_restore_target_focuses_without_closing_overlay` (no `closewindow` dispatch, exactly one `focuswindow` call) and `test_restore_target_noop_without_a_target`.

## ADR-020 — Post-run feedback: cheap ratings, no-GitHub neutral list, context-attached bug intake (2026-09-09)
- **Context:** A-005 asked for a quick way to rate a finished run (+/neutral/-) so feedback doesn't require typing a full `/report`. Reusing the existing bug-intake flow (ADR-016) for a thumbs-down button posed one problem: `start_intake`/`gather_context` always built context from "the run immediately before this one" (`last_run_info`, indexed by `RUNS` insertion order) — correct for the manual `/report` flow (the user is describing whatever they just did), but wrong for a feedback button, where the run being rated and the run the button's own request creates are two different `run_id`s and nothing guarantees adjacency in `RUNS`.
- **Decision:** `POST /v1/runs/<id>/feedback` (`handle_feedback` in `brain/server.py`) accepts `{rating: good|neutral|bad}` only for a run in a terminal (`done`/`error`) state, and only once per run (a second rating is a 400, matching "cheap, no re-litigating"). Good/neutral just set `run['feedback']` and write a `feedback` journal record (a fifth, optional phase after the usual prompt→process→done→eval — see `docs/LOGGING.md`); neutral additionally appends a redacted line to local-only `logs/feedback/needs-review.jsonl` — deliberately not a GitHub issue or `docs/backlog` entry, so a stream of "didn't fully work, don't want to dig in now" clicks can't spam the tracker the way `/report` intentionally can. Bad reuses `start_intake` unchanged in shape, but `last_run_info` was refactored into `run_info(run_id, run)` (a pure lookup-and-format, no ordering) plus `previous_run_id(current_id)` (the old ordering logic, now isolated); `gather_context`/`start_intake` gained an optional `source_run_id` that, when given, points context at that exact run instead of computing "whatever's previous." The feedback handler mints a fresh `run_id` (same shape as any other `/v1/run` POST — new `RUNS` entry, `BUSY` lock, `prompt` journal record) and starts `start_intake(new_run_id, 'bug', seed, target, source_run_id=rated_run_id)` with `seed` set to the rated run's own original prompt text, so the resulting title/questions describe the thing that actually went wrong, not a re-derived guess from chat text.
- **Consequences:** The manual `/report`/`/feature` NL-trigger path is untouched (its call sites simply don't pass `source_run_id`, so `previous_run_id` still drives it exactly as before). `run_info`'s output shape (`jarvis_version`/`last_run_id`/`last_plan`/`last_approval`/`last_log_path`/`log_excerpt`) is unchanged either way — only *which* run it describes differs by caller. The overlay (`overlay/app.js`) shows a small 👍/🤔/👎 row once a run reaches `done`/`error` and hides it once `result.feedback` is set; 👎 hands the returned `run_id` straight to the existing `poll()` loop, so the bug-intake Q&A appears in place exactly like any other run. No new approval bypass: filing the resulting bug report still goes through the same `report_bug` plan → approve/deny path as every other issue. Covered by `tests/test_jarvis.py::FeedbackTest` (good/neutral/bad, one-rating-per-run, busy-run rejection, and that the bad-path context references the rated run specifically) and `tests/overlay.test.cjs` (button visibility/hide, POST payloads, thumbs-down handoff into the intake Q&A panel).

## ADR-021 — Automatic log retention plus a manual temp-cleanup script (2026-09-09)
- **Context:** `logs/` is gitignored (`git ls-files logs/` is always empty — nothing here was ever tracked), but it grows unbounded on disk: one file per run under `logs/runs/`, one per run under `logs/debug/` when `log_level=debug`, one archive per `VERSION` bump under `logs/journal/archive/`, plus assorted one-off scratch (`scripts/demo-test.py`/`scripts/verify-host.py` evidence dumps, ad-hoc manual redirects like `clients-after-*.json`, `__pycache__`). Left alone indefinitely this both wastes disk (the Chromium `logs/overlay-profile/` cache alone reached 150MB+ on this host) and makes `logs/` noisy to skim for the run journal's actual purpose (ADR-018).
- **Decision:** Two independent mechanisms, matching the two different growth patterns. (1) **Automatic, unconditional retention** for the three directories that grow exactly one file per event: `brain/journal.py`'s `prune(directory, pattern, keep)` deletes the oldest files (by mtime) beyond a cap — `RUN_LOG_KEEP=200` for `logs/runs/*.log`, `DEBUG_KEEP=200` for `logs/debug/*.jsonl`, `ARCHIVE_KEEP=20` for `logs/journal/archive/*.jsonl`. It's checked once per *new run* (the `prompt` phase of `Journal.write`) for the first two, and immediately after each version-bump rotation for the third — never on a poll or a per-debug-event write, so retention can't become the hot-path cost ADR-018 explicitly ruled out. `logs/feedback/needs-review.jsonl` (ADR-020) is deliberately exempt — it's a short list meant for a human to read and clear, and auto-deleting unreviewed entries would defeat that. (2) **Manual, explicit cleanup** for everything else: `./scripts/clean-temp-logs.sh` removes a documented allowlist of one-off scratch filenames plus `__pycache__` anywhere in the repo, optionally (`--profile`) also `logs/overlay-profile/` — gated on a live `hyprctl`-based check (not a fragile `pgrep -f` command-line match, which false-positived on this host by matching the checking shell's own command text) that no overlay window is currently open, since deleting a running Chromium profile's directory out from under it would be disruptive.
- **Consequences:** `.gitignore`'s existing bare `logs/` line already fully covered every path here — no `.gitignore` change was needed, and there was nothing tracked to `git rm --cached`. `clean-temp-logs.sh` is not wired into `doctor.sh` (a non-destructive health check by design) or a git hook (optional per-clone, not mandatory); it's documented in `README.md`/`docs/LOGGING.md` as something to run yourself before a commit if you want a tidy tree. Verified live on this host: ran the script for real, removed 5 stray scratch files plus 4 `__pycache__` dirs, confirmed the overlay still launches/toggles/closes correctly after `--profile` cleared its 150MB+ Chromium profile cache. Covered by `tests/test_journal.py` (`prune` keeps-newest/deletes-oldest, no-ops on a missing directory, does nothing under the cap; `Journal.write` prunes run logs only on a `prompt` phase and prunes the archive only on rotation) — the shell script itself is exercised live rather than unit-tested, consistent with this repo's existing pattern for host-only scripts (`scripts/verify-host.py`, `scripts/demo-test.py`).

## ADR-022 — Overlay lifecycle bugs: input-wipe root cause, restore-on-reopen, single-instance hardening (2026-09-09)
- **Context:** Real usage after A-004/A-005 surfaced four problems (A-010, Alex's own report): typing into the Q&A/report box got wiped almost instantly; reopening the overlay after closing it always started from a blank "Ready" screen even though the server still had the last run's full state; the hotkey's "already open somewhere" behavior felt like it might be a second, unreachable copy; and the original prompt wasn't shown next to the new (A-005) feedback controls, so it wasn't obvious what they applied to.
- **Decision:** Root-caused the input-wipe first, since it was reproducible from reading the code alone: `poll()` re-fetches and calls `render(result)` every 700ms while a run is in progress — that's what makes A-004's live step list work — but `render()` unconditionally reset `qaAnswer.value`/focus and `runBtn`'s focus on *every* call, even when the question/plan hadn't actually changed, wiping out whatever the user had typed roughly once a second. Fixed by tracking a cheap signature of the parts of `result` that matter (`status|reply|plan|draft`) in `lastRenderKey`, and only doing those specific destructive resets on a genuine transition into a new question/plan — reset once via `setConsoleTarget` whenever the tracked run id actually changes, so a fresh run never inherits a stale key. Separately, the submit handler now clears `input.value` immediately on send (restoring it only if the request itself fails) instead of leaving stale text sitting in a disabled box. For restore-on-reopen: the overlay's startup script now does more than wire up the Open-console button for `localStorage`'s last-run id — it fetches `GET /v1/runs/<id>` and calls the same `render()` immediately, so a reopened overlay shows the last reply/steps/feedback controls instead of "Ready"; if that run is still in progress (the hotkey's close path, unlike Escape, doesn't deny a pending plan first), it resumes polling instead of leaving it to the 900s timeout unseen. For single-instance: `core.is_overlay()` now matches any class *containing* `jarvis-overlay`, not just the two literal variants known from ADR-013, so a Chromium relaunch under a class variant we haven't hardcoded yet is still recognized; `toggle-overlay.py` additionally self-heals if it ever finds more than one overlay-class window (closes every extra one, keeping whichever is focused or the first) instead of leaving the user with a "second mysterious copy" they can't find. Last, the feedback controls now show the original prompt text (`result.prompt`, already present in run state since A-005) above the 👍/🤔/👎 row.
- **Consequences:** No new auto-dismiss timer was added — "the window should go away after a task finishes" is read here as *the existing dismiss affordances (Esc, re-pressing the hotkey while focused) plus not losing state when you do*, not a new automatic timer, since a blind timer risks recreating exactly the "blink and it's gone" problem ADR-019 just fixed, and there's no principled duration to pick. If Alex wants literal auto-dismiss on top of this, that's a distinct, separable follow-up (a new assignment), not blocking this one. Live-verified on this host: a genuinely duplicate/self-heal scenario couldn't be reliably reproduced through this sandboxed session (spawning the overlay here doesn't grant it real window focus the way a user's keypress would — the same documented limitation as `scripts/verify-host.py`'s existing notes), but the close/focus/reopen branches were each verified individually via `hyprctl` (open-when-none-exists, focus-when-not-focused, and — after manually granting the overlay real focus via `dispatch('focuswindow', …)` — close-when-focused all behave correctly), and the full run→approve→done→(close)→state-available-for-restore path was verified end to end against the real service. Debugging this fix hit what looked like a genuine infinite hang in `tests/overlay.test.cjs` (documented in PROGRESS.md); it turned out to be a pre-existing assertion reading `elements['#prompt'].value` *after* submission, which started failing the moment the input-clear fix landed — its crash report was starved by an unrelated orphaned background poll chain already latent in the test mock's synchronous `setTimeout`, making a plain assertion failure look like an infinite loop. Covered by `tests/test_jarvis.py` (`is_overlay` known/unseen-variant matching) and an extended `tests/overlay.test.cjs` (repeated identical poll of an unanswered question no longer clears it; input clears on send; a second, isolated vm context with a pre-populated `localStorage` last-run id restores the terminal reply, steps and feedback prompt on load instead of starting blank).

## ADR-023 — Open any installed app by name via omarchy-launch-or-focus + .desktop resolution (2026-09-09)
- **Context:** Saying "spotify" produced an empty plan and "not a supported action" — `open_webapp` only covers four fixed webapps (Todoist/Google Calendar/Outlook/WhatsApp); there was no path at all for arbitrary installed desktop apps. Alex asked for a *general* mechanism (Spotify is the acceptance fixture, not a special case), and the host turned out to already ship exactly the right building blocks: `omarchy-launch-or-focus <window-pattern> <launch-command>` (focuses a window matching a `hyprctl`/`jq` regex if one exists, else `eval exec`s the launch command) and per-app `.desktop` files under the standard XDG locations, whose `StartupWMClass` field is the authoritative window-class Hyprland will actually assign (more reliable than guessing from `Name=` or the `Exec=` binary).
- **Decision:** New `actions/open_app_by_name` (+ `open_app_by_name` tool) resolves user text against installed `.desktop` entries (`~/.local/share/applications`, `/usr/share/applications`, both Flatpak export dirs; `NoDisplay=true` and non-`Application` types skipped; first directory wins on a duplicate stem) via `core.resolve_app()`: exact match wins outright, else an *unambiguous* prefix/substring match, else the single closest `difflib` fuzzy match (cutoff 0.6); a genuine tie raises listing the candidates rather than guessing. The resolved entry's `Exec=` line has its field codes (`%u`/`%f`/...) dropped — the *whole* token, not just the code, so `--uri=%u` disappears instead of leaving a dangling `--uri=`, since a by-name launch has no file/URL to fill them with. The actual launch/focus is entirely delegated to `omarchy-launch-or-focus <StartupWMClass> "uwsm-app -- <cleaned Exec>"`, invoked via `Popen` (never `command()`/`subprocess.run`, since a real launch reaches that script's `eval exec setsid …`, which *replaces* the process — waiting on it synchronously would block until the launched app itself exits). After invoking it, `open_by_name()` polls `hyprctl clients` (up to 20s) for a class/title match to report back, since the shell wrapper does the actual dispatch itself.
- **Consequences:** No new window-polling/matching logic duplicated what `open_app()` (webapps) already does — reusing `omarchy-launch-or-focus` instead means Jarvis doesn't need its own "does a matching window already exist" branch at all for this tool, unlike `open_webapp`. Scope deliberately stops at `.desktop` resolution: known webapps keep using `open_webapp` (the system prompt tells the model which is which) and catalog bindings keep using `run_binding`, rather than one tool trying to cover all three sources the way the source assignment's phrasing loosely suggested — duplicating resolution logic across tools seemed worse than a one-line system-prompt disambiguation. `open_app_by_name` is a plan action like any other — still gated behind approve/Run, not auto-run. Verified live on this host: `./actions/open_app_by_name --name spotify` for real launched Spotify (previously not installed as a running window), reported its address/class/workspace correctly, and a second call correctly focused the same window instead of relaunching a duplicate — `omarchy-launch-or-focus`'s own existing behavior, exercised rather than reimplemented. Covered by `tests/test_jarvis.py::OpenByNameTest` (NoDisplay/non-Application filtering, duplicate-stem precedence, exact/prefix/fuzzy/ambiguous/no-match resolution, field-code stripping including the whole-token case, dry-run argv, window-found and timeout paths, `tool_argv`/`action_label` wiring) using synthetic `.desktop` fixtures and mocked `hyprctl`/`Popen` — not the real filesystem or window manager.

## ADR-024 — YouTube webapp fallback + a planner honesty guard against empty-action success claims (2026-09-09)
- **Context:** Run `fad6f832` shipped `{"actions": [], "reply": "Opening YouTube."}` for the prompt "open youtube" — a zero-action plan skips approval entirely (`commit_plan`'s own zero-action branch, by design for chitchat), so this false claim reached the user as an already-`done` result with nothing to review first; Alex rated it "bad" via the A-005 feedback controls. Reproducing live on this host surfaced a related, distinct failure too: the same prompt sometimes gets a *real* action for the *wrong* target (a plan to open Google Calendar), a plain qwen2.5:3b reliability gap this pass can reduce with a concrete example but can't eliminate.
- **Decision:** Two independent fixes. (1) `YouTube` added to `open_webapp`'s existing `APPS` map (`actions/core.py`) rather than relying only on A-011's `.desktop` resolution — `open_app()` never actually checks for an installed `.desktop` file, it always opens the URL directly via `omarchy-launch-webapp`, so this gives the literal "webapp if a window already exists, else a browser tab" fallback the assignment asked for regardless of whether a local `.desktop` shortcut is present on a given host (A-011's `open_app_by_name` would instead correctly raise "No installed app matches" on a host without one — the right behavior for a general app launcher, wrong for a should-just-work-anywhere web destination). A new `open_webapp` JSON-plan example points the model at it. (2) `FALSE_ACTION_CLAIM_RE` in `brain/server.py` — a reply that *opens* with an action-claiming verb ("Opening…", "Moving…", "Switching…", etc., matching this project's own established reply style for real actions) while `actions` is empty gets rewritten to an honest "I don't have a way to do that yet" before the run ever reaches the user, in both `json_plan()` (the default planner) and `plan_tools_run`'s no-tool-calls branch (the opt-in `JARVIS_PLANNER=tools` loop) — same bug shape, same fix, both planner surfaces. A genuinely honest empty-action reply ("Hello! How can I help?") is untouched — the regex only matches the specific lying pattern, not "any reply with no actions."
- **Consequences:** No new resolver/window-polling code for YouTube specifically — reusing `open_webapp`'s existing, already-tested launcher was strictly simpler than extending A-011's `.desktop`-only resolver to also do a URL fallback. The honesty guard is a regex heuristic, not a semantic check — it can't catch a false claim phrased with different wording, and the "Opening Google Calendar" wrong-target hallucination (a real action, just the wrong one) is unaffected by it; that class of error is journal-flagged as `suspicious`/`mismatch` after the fact (ADR-018) rather than prevented before the user sees it, and remains an open, harder problem this pass doesn't claim to solve. Verified live on this host: `open_webapp --name YouTube` really opened YouTube (class `brave-www.youtube.com__-Default`) and a second call correctly focused the same window rather than duplicating it; reproduced the original empty-action lie against the live service before the fix landed (`open_webapp` enum didn't yet include YouTube, so "open youtube" hit `json_plan()`'s general failure path) to confirm the bug was real, not just the journal record. Covered by `tests/test_jarvis.py`: `test_false_action_claim_with_no_actions_is_rewritten_honestly` (the exact `fad6f832` reply), `test_honest_empty_reply_is_left_alone`, `test_false_action_claim_reply_only_checked_when_actions_empty`, `test_plan_tools_run_rewrites_false_action_claim_too`, `test_youtube_is_a_webapp_independent_of_any_installed_desktop_file`.
## ADR-025 — Isolated worktrees for concurrent agents (2026-09-09)
- **Context:** A-011 encountered another agent's uncommitted A-007 work on `codex/training-track` in the shared checkout. A separate worktree based on `main` let it ship without checkout/stash collisions. A-013 makes that successful practice the default.
- **Decision:** Each concurrent coding agent must use a separate `~/Work/omarchy-jarvis-<slug>` worktree and dedicated branch from `origin/main` (or an agreed base). Reuse explicitly assigned isolated trees. Serial work can keep the canonical checkout. START and all coding-agent prompts contain exact creation/push/cleanup commands; no helper is needed. Status prints worktree/branch/dirty hints without file contents or mutations.
- **Consequences:** Working files and index are isolated, but refs, live services and desktop are shared. Area/path restrictions and single-writer brain policy still apply. QUEUE/SESSION are branch-local snapshots, so desk claims must be reconciled with other worktrees read-only before starting, and bookkeeping reconciled on merge. Never stash/reset/checkout another worker's tree; remove only clean, merged, unowned trees after handoff. Hints do not authorize claims or spawn agents.

## A-014 hygiene — bounded honesty guard (2026-09-10)
Empty plans use one conservative lexical guard for both planners. The fallback says only that no actions ran, without inferring that a capability is missing. Tools execution reports completed labels rather than model-written completion prose. This does not verify arbitrary natural-language claims or desktop effects. VERSION 0.4.2 deployment/restart is deferred to the shared-service owner so isolated hygiene work does not disrupt the overlay track.

## A-014 hygiene — experimental tools approval boundary (2026-09-10)
The opt-in tools executor now executes only the initial reviewed calls. If the follow-up model response asks for more tools, it stops with a partial/error result and asks for a new request; it cannot silently expand the approved plan. The old six-turn execution loop is removed. Both planner validators also reject report_bug/report_feature even if the model returns them despite their exclusion from its schema; deterministic intake still produces those reviewed actions. This is a fail-closed restriction, not a new approval protocol.
## ADR-026 — Extensible slash registry
**Context:** command discovery must precede Training mode without changing execution.
**Decision:** serve a static commands.js registry, filter prefixes in the overlay and complete
with keyboard/mouse. Completion inserts text only; a separate submit follows existing routes.
**Consequences:** no network calls per keystroke, accessible listbox semantics, and first Esc
closes suggestions before the overlay. New deterministic commands must update this registry.

## ADR-027 — Training prepares confirmed local work
**Context:** improvement context was spread across issues, journals and assignment files.
**Decision:** an authenticated, on-demand dashboard exposes bounded evidence and local agent
slots. Preview freezes exact file contents; an explicit single-use confirmation checks
source fingerprints before creating an assignment/handoff or updating slot metadata.
**Consequences:** no extra run-poll I/O or cloud spending. GitHub outages retain local context.
Busy slots support a persistent local queue, not automatic sending. Real agent presence is
unknown. Preparing work does not claim ownership; the receiving agent still reads SESSION.

## ADR-028 — Human validation definitions and evidence
**Context:** CI and tool return codes cannot establish that desktop behavior met Alex's intent.
**Decision:** keep JSON feature guides with explicit steps/expected outcomes, and record human
Verify/Fail only through a confirmed preview. Hash guide definitions to invalidate stale
validation, retain tested version/revision/notes, and generate FEATURES from the catalog.
**Consequences:** automated checks never manufacture human validation. Failures remain local
until the user drafts and approves an issue through existing intake. Source run context is
explicit, avoiding attribution to an unrelated recent run. Verification auto-closes nothing.

## ADR-029 — A-015: deterministic, description-backed plan/step labels for skill recipes (2026-09-10)
- **Context:** Alex validated feat-overlay-chat on 0.5.2 and noted that asking Jarvis to run a
  skill recipe (e.g. "open my planning in a new workspace") showed almost no detail before or
  during execution — just the bare skill slug, not what it would do or with what parameters,
  and "poorly readable for a human." Training turned that note into A-015, scoped to `skills/`
  only. Tracing the actual text showed the root cause was `action_label()`/`json_plan()`'s
  model-written `reply` in `brain/server.py`, both outside that scope — a `skills/`-only change
  (e.g. richer `SKILL.md` prose) could not have altered what's displayed, since nothing read
  `SKILL.md` at runtime. Asked Alex directly; scope was explicitly expanded to
  `brain/server.py` + `brain/system_prompt.md` rather than shipping a no-op fix or leaving A-015
  blocked.
- **Decision:** `action_label()` now appends a `run_skill` action's own `SKILL.md`
  `description:` front matter to its slug — this label is what both the live step list
  ("Running open-planning — Open Todoist, …") and the final "Completed: …" message use.
  `json_plan()` deterministically overwrites a single run_skill action's model-generated
  `reply` with that same description before it reaches the pre-approval preview headline,
  rather than trusting a 3B model to reliably describe a skill recipe on its own (consistent
  with this codebase's existing pattern of not trusting model prose for user-facing claims —
  see ADR-024's honesty guard). A new `summarize_step_result()` parses a finished run_skill
  step's raw JSON stdout (`{'skill':…, 'results':[…]}`) into a plain sentence ("Created
  workspace 3; opened Todoist; opened Google Calendar.") instead of a truncated JSON dump,
  falling back to the raw (truncated) stdout for any other tool or an unrecognized shape.
  No other tool's label/reply path, `overlay/`, or `actions/` changed.
- **Consequences:** Every place the user sees a skill-recipe plan or its outcome — preview
  headline, running step, completion message — now names what it actually does, without a
  model round-trip in the loop for that text. `feat-overlay-chat`'s `jarvis_version_shipped`
  moved to 0.5.3 (VERSION bumped to match) and it gained a fifth guided step exercising a
  skill-based plan preview; `definition_hash` correctly flips its recorded validation back to
  unvalidated, so Alex re-tests the actual fix rather than it silently reading as already
  covered by the 0.5.2 validation that reported this exact problem. Covered by
  `tests/test_jarvis.py`: `test_skill_description_reads_skill_md_front_matter`,
  `test_action_label_names_a_run_skill_action_with_its_description`,
  `test_summarize_step_result_turns_skill_json_into_a_sentence`/`_falls_back_on_unrecognized_shape`,
  plus `test_valid_complete_recipe` and `test_success_reply_comes_from_executed_tool` updated
  to expect the richer text (109 Python tests pass). Not exercised: the real overlay/Ollama
  end-to-end path (no live Hyprland/model in this sandbox) — Alex should restart
  `jarvis.service` from `main` and re-run feat-overlay-chat's step 5 on 0.5.3.

## ADR-030 — Separate Training window and durable problem triage (2026-09-10)
- **Context:** the inline Training dashboard mixed evidence, validation and work preparation in chat's small window.
- **Decision:** one brain serves two UIs. Chat's Open Training and /train call an authenticated fixed launcher; a separate Chromium profile/class opens /jarvis-training. The launcher focuses an existing Training window, addresses only its own window when floating/centering, and never starts/restarts shared services. No global hotkey/config edit is needed.
- **Panels:** Problems is the default list/detail surface; Validate and Assignments / Agents use the same chrome with distinct panels. Assignment and agent redesign beyond this structure remains A-017/A-018.
- **Persistence:** on-demand evidence imports use ignored logs/training/problems.json with atomic replacement under Training's lock. Stable source ids preserve original bounded/redacted context, edits, P0–P3 priority and open/done/dismissed status across refreshes and journal rotation. Confirmed local deletion leaves a tombstone; original sources are untouched. The dashboard now writes new evidence snapshots when importing, but does no extra run-poll I/O.
- **Review:** direct Save/Done/Dismiss express local triage intent; revision hashes reject stale updates. Assignment generation reads the saved problem and includes its priority and original evidence, with the existing exact-byte preview and stale-source confirmation checks. Priority never overrides QUEUE/SESSION ownership. Human validation stays unvalidated until Alex performs it.

## ADR-031 — Reviewed assignment editing and explicit draft generation (2026-09-10)
- **Context:** A-016 separated Training but assignments were still a title dump and generation was coupled to choosing an agent slot.
- **Decision:** Assignments gets a queue/list/detail editor; Agent monitor has its own nav panel. New drafts optionally inherit a saved problem and retain its original evidence on save. Save/Add previews exact brief/QUEUE/INDEX changes and a separate SESSION preparation note; confirmation preserves active ownership and rejects stale inputs. Queued/blocked briefs can be edited, while in-progress/closed briefs, status and parallel policy remain owned by the existing workflow. Queue order and claiming semantics are unchanged. Unknown brief sections are retained; full original Markdown remains inspectable.
- **Generation:** explicit local Ollama drafting uses schema-constrained JSON, a bounded response, selected scope, no tools, and model-info preflight rejecting remote/cloud models. It never writes, claims or dispatches. The alternative on-machine-agent mode prepares a copyable prompt and accepts a pasted JSON draft; the user chooses/runs that agent and is told it may use paid services. No launch or automatic spend occurs. Reviewable text is always editable before Save/Add.
- **Handoff:** saving a brief does not create a slot. Hand off to agent navigates to Agent monitor with the saved assignment selected; the existing confirmed prepare/queue/paste workflow follows. A-018 owns live tiles/windows.
- **API references:** [Ollama structured outputs](https://docs.ollama.com/capabilities/structured-outputs) and [official ShowResponse metadata](https://github.com/ollama/ollama/blob/main/api/types.go) (`remote_host`, `remote_model`, `model_info`). Generation relies on the trusted local daemon; no model pulls or global Ollama configuration changes are made.

## ADR-032 — Agent monitor: visible windows over hidden background jobs (2026-09-10)
- **Context:** A-018 asked for a way to see and steer local coding agents without them living in the background — Alex explicitly rejected slot metadata alone as the main UX; he wants the same window he'd get launching and pasting a prompt into a terminal himself. A-017 left slots as a flat status list with no way to actually open one.
- **Decision:** `scripts/open-agent.py` (mirroring `scripts/open-training.py`'s launch-or-focus shape) opens/focuses one real terminal window per agent slot via `omarchy-launch-or-focus-tui --app-id=jarvis-agent-<slot-id> <command>` — the same Omarchy primitive ADR-023 already uses for webapps, here wrapping a TUI. `claude-code` → `claude`, `cursor` → `codex` (the CLI actually installed for that slot's "Cursor / Codex" label); `human` slots have no process to launch — Alex works those in his own terminal. A new `POST /v1/training/agent-window` route resolves the slot server-side (`training.find_slot`) and passes its *recorded* kind to the script, never a client-supplied one, before shelling out with a bounded timeout exactly like the existing `/v1/training/open` route. The Agent monitor panel gets a tile grid (click → detail overview: status, current/queued assignments, last handoff, Open agent window, the existing idle/busy toggle) and a read-only queue board naming each assignment's current worker, sourced from the same `dashboard()` data already fetched — no new read endpoint. Confirming a "prepare now" handoff auto-opens the resulting window; the detail panel's own button covers reopening one later.
- **Consequences:** No auto-typed/pasted prompt into the launched terminal (out of scope — "paste-ready or auto-pasted" is satisfied by the existing Copy handoff clipboard flow plus a guaranteed-visible window); a human still pastes it themselves. Multiple slots can have simultaneously open, individually addressable windows since each gets its own app-id/class; a rapid second click on the same slot focuses rather than relaunches (a per-slot lock file guards the brief launch-to-window-appears window). Assignments/Problems CRUD (A-016/A-017) and chat plan detail (A-015) are untouched. Not exercised end-to-end: no live Hyprland/terminal in this sandbox, so the launch/focus path is unit-tested (mocked `hypr`/`dispatch`/`Popen`) rather than verified against a real window; `feat-agent-monitor` validation guide is added unvalidated for Alex. Covered by `tests/test_agents.py` (script: focus-existing vs launch-new argv per kind, human-kind rejection, invalid slot id, window-never-appears; HTTP route: kind resolved from the registered slot not the request body, unknown slot rejected before any launch, script failure surfaces as 409) and `tests/agents.test.cjs` (tile rendering, detail overview, human slots hide the launch button, Open agent window POST, idle/busy toggle, queue board worker names).

## ADR-033 — Validate features: visible run ids, auto-run mechanical steps, obvious persistence (2026-09-10)
- **Context:** Alex: Verify/Fail didn't clearly leave the pending list, the guided-step run-id field asked for something Jarvis never showed him anywhere, and every mechanical step ("open Jarvis, type X, press Enter") was busywork he had to perform by hand before he could even get to the part that needed his judgment. Auditing `training.preview`/`confirm` and `tests/test_validation.py::test_result_requires_confirm_and_records_version` showed the persistence itself already worked end to end — the guide just stayed open afterward with the same checked boxes, so a real write looked like nothing happened.
- **Decision:** Three independent fixes, each addressable on its own. (1) **Obvious persistence:** `validationSaved()` now closes the guide back to the list on a confirmed result instead of leaving it open; `renderValidations()` prints each item's last report (result/date/version/notes) inline in the list itself, so it's visible without clicking in — "not only re-run empty forms." (2) **Run id:** the chat overlay now shows the current run's id in the footer (`#run-id-btn`, click to copy) — it was already used internally for console/feedback but never surfaced to a human; the field itself is relabeled to make "leave it blank" explicit. (3) **Auto steps:** a catalog step is now either a plain string (`human` — a UI action or judgment call, unchanged) or `{"text","kind":"auto","prompt"}` (`validation.step_text`/`step_kind`, schema-checked in `validation.load`) — an auto step gets a **Run this step** button in `validation.js` that posts the literal `prompt` to the same `/v1/run` a chat send uses, polls `/v1/runs/<id>` the same way the overlay's own poll loop does, shows the observed reply/status inline, and captures the run id into the evidence field automatically. This never touches Cancel/Run/approval — a plan that reaches `awaiting_approval` is left exactly there for Alex to decide in the real chat overlay; the step's own checkbox still needs an explicit human tick after reading the result. `feat-overlay-chat`'s three "Type: ..." steps are migrated to `auto` as the reference case; guides whose mechanical steps are pure Training-window UI navigation (not a chat prompt) have no `auto` candidate under this mechanism and stay `human` for now.
- **Consequences:** No new endpoint was needed for auto steps — they reuse `/v1/run` and `/v1/runs/<id>` exactly as the chat overlay does, so approval/denial, busy-run conflicts and the honesty guards (ADR-024) all apply unchanged; a step run while another chat run is busy fails with the same "not available" error Alex would see in chat. `feat-overlay-chat` and `feat-validate` both got new guided steps (and jarvis_version_shipped bumps), so `definition_hash` correctly re-flips them to unvalidated for a fresh human pass rather than silently inheriting old evidence. Covered by `tests/test_validation.py` (mixed string/object steps load and hash correctly, an auto step without a `prompt` is rejected, `report_evidence`'s seed still joins mixed-shape steps) and `tests/validation.test.cjs` (auto step exposes exactly one Run button, clicking it hits `/v1/run` then `/v1/runs/<id>` and fills the run id, a confirmed result closes the guide, the list shows an inline report) and `tests/overlay.test.cjs` (the run id becomes visible once a run has one). Not exercised: a real live Ollama/Hyprland round trip for an auto step (no live service in this sandbox) — Alex should click Run this step for real on `feat-overlay-chat` once merged.

## ADR-034 — Assignment scope is area + worktree, not hard path fences (2026-09-10)
- **Context:** Alex: `Allowed paths:`/`Forbidden paths:` as hard gates forced constant scope-expansion asks for one-line fixes next door, and worktree+branch isolation (ADR-024) already makes each concurrent agent's checkout independent regardless of which files it touches. Concretely wrong in practice this session: `docs/assignments/prompts/NEW_AGENT.txt`'s claim rule read "disjoint area/paths," which a coding agent (this one) read as *area OR paths* and used to justify claiming A-021 (`area:overlay`) while A-020 (`area:overlay`) was still `in_progress`, reasoning their path lists didn't overlap. Both landed real, good changes in separate worktrees, but merging them back into the canonical checkout produced conflicts across the shared bookkeeping files (`docs/PROGRESS.md`, `docs/SESSION.md`, `docs/assignments/{QUEUE,INDEX}.md`) that a second session had to stop mid-merge to sort out — direct evidence that path-list disjointness was never actually sufficient for "safe to parallelize" once shared docs are in play, only area disjointness is.
- **Decision:** Isolation for parallel assignment/issue work is **git worktree + branch** (already mandatory, ADR-024) plus a **different `area:`** from every `in_progress` row — not a path allowlist. `Allowed paths:`/`Forbidden paths:` (assignments) and `Allowed paths:`/`Forbidden paths:` (GitHub issues, START's Issue Loop) become **optional soft hints** for a reviewer's context, never a claim gate and never a reason to block or expand-and-ask before touching a one-line fix next door that's still in-repo, in-area, and doesn't violate the assignment's own out-of-scope or plan→approve→execute. `TEMPLATE.md`, `docs/assignments/README.md`, `START.md`'s Parallel work section, and all three paste prompts (`NEW_AGENT`/`CONTINUE`/`PARALLEL`) are reworded so "claimable in parallel" reduces to one unambiguous check: does this row's `area:` differ from every `in_progress` row's `area:`? Training's Assignments editor (`overlay/training.html`, `brain/training.py::assignment_fields`) drops the HTML `required` attribute and the corresponding non-empty validation on `allowed_paths`/`forbidden_paths`, and relabels the fields "(optional hint, not a gate)"; the field names/labels in existing and desk-generated briefs are otherwise unchanged for parsing compatibility. `scripts/assignment-status.sh`'s claim-hint branch now actually computes area disjointness against every `in_progress` row instead of only filtering on `parallel-ok: YES` (which is what let A-021 look claimable in the first place) — it never inspected paths and never will; that job now falls entirely to the human/agent judgment call on the goal/checklist/out-of-scope, not a machine-checked path list.
- **Consequences:** An agent no longer needs to open the in-progress row's `active/*.md`, parse its path list, and reason about overlap before claiming parallel work — one area comparison, already visible in QUEUE.md, is enough. This is strictly more conservative than the old (buggy) path-based reading: some genuinely non-overlapping same-area work that could have safely run in parallel under careful path bookkeeping is now serialized instead, which is the intended trade favoring fewer merge conflicts on shared docs over maximum parallelism. Historical `done/` assignments keep their old hard-sounding "(required if parallel-ok YES)" wording verbatim — out of scope to rewrite, and harmless since they're closed. Product code changes were kept to the minimum needed to stop the UI/validation layer from re-hardening the same gate (dropping `required` + the non-empty check); no change to how worktrees are created, to `area:`'s five values, or to `single-writer` defaults for `area:brain`. Covered by `tests/test_assignments.py` (blank `allowed_paths`/`forbidden_paths` now save successfully) and a manual run of the updated `scripts/assignment-status.sh` against a synthetic same-area in-progress/queued QUEUE.md, confirming the same-area row is now correctly excluded from the claim hint (the exact scenario that mis-fired for A-021).
## ADR-035 — Proposed-action bubble carries the plan, not a below-bubble text dump (2026-09-10)
- **Context:** A-015 made a `run_skill` plan's `reply` genuinely descriptive (e.g. "Open Todoist, Google Calendar, Outlook and WhatsApp together..." instead of the bare skill slug), but it only ever reached Alex via `#status` — a single-line, `text-overflow:ellipsis`-truncated element positioned *below* the actions list — while the list itself (`describeAction()`) showed nothing but a bare tool/skill name plus raw `key=value, key2=value2` soup for any other argument. Alex had to read two disconnected, differently-formatted places to understand one plan, and the one with the real detail was truncated.
- **Decision:** `renderPlan()` now builds each action as a `.action-card` (title + optional description + chips) instead of a single `<li>` text node. `actionTitle()` maps each of the 12 known tools to a friendly verb phrase using its most important argument (`ACTION_TITLES`/`TITLE_ARGS` in `overlay/app.js`, mirroring — but not sharing code with — `action_label()`'s tool knowledge in `brain/server.py`, since the client only ever sees `plan.actions`, not a server-computed label); any argument not already folded into the title becomes a small, individually truncating chip (`title=` attribute carries the full value for hover) rather than raw prose. For `run_skill` specifically, `plan.reply` (the A-015 description) becomes the card's own description line — the one place it's authoritative. `#status` during `awaiting_approval` now shows a fixed "Review the plan below." instead of repeating `result.reply`, retiring the redundant below-bubble dump the assignment named directly.
- **Consequences:** Bubble content is capped at the tools `ACTION_TITLES` actually maps (A-025 added `correct_app_open`); an unmapped future tool falls back to its prettified `tool_name` with no chips lost (same args, just under a less friendly heading) rather than erroring. The live step list (`renderSteps`) is untouched — it already renders server-computed `action_label()` text from A-015, which was the right fix for *that* surface and stays consistent in spirit (friendly description, not a slug) without needing a duplicate client-side mapping. Found and fixed in the process: `tests/overlay.test.cjs`'s DOM mock never actually cleared `.children` on `innerHTML=''` (unlike every other test file's mock) and the mocked `setTimeout` fires synchronously with no real delay, so a background poll chain outliving a thrown assertion — previously masked because the assertions never failed — became a genuine unbounded-growth hang the moment bubbles became multi-node instead of one text node; fixed the mock's `innerHTML` setter and hardened the test's outer `.catch` to `process.exit(1)` instead of silently hanging past a real failure. Covered by `tests/overlay.test.cjs`: bubble title/description content for a `run_skill` plan, chip rendering (and which args a title already consumes) for a `report_bug` draft, and the retired `#status` duplication.

## ADR-036 — App-open preference weights and deterministic “the other one” correction (2026-09-10)
- **Context:** A-024 made an ambiguous `open bitwarden` open the top-ranked `.desktop` match instead of hard-failing. Alex still needed the rest of #16: after a wrong top match, say **“no, the other bitwarden”** (or “the other one” / “not that one”), close the just-opened window, open the next match, and remember that choice so the next `open bitwarden` prefers it. A 3B JSON planner cannot be trusted to compose close+open (ADR-011 caps a plan at one action) or to know which window Jarvis just launched.
- **Decision:** Preference state is a small JSON file at `~/.config/jarvis/app-preferences.json` (XDG, same convention as `config.toml` / ADR-015 — not repo-tracked). Keys are the normalized query plus desktop stems; integer weights break ties *inside* A-024's existing exact > prefix > substring > fuzzy tiers, with `desktop_entries()` order as the remaining tie-break. Successful `open_app_by_name` records `last_open` (query, stem, wmclass, window address, timestamp) with a 15-minute TTL. Phrases matching `APP_CORRECTION_RE` are intercepted *before* bug/feature intake and the JSON planner, the same way `/backlog` is. That router is the only writer of a new reviewed tool `correct_app_open` (excluded from `TOOLS_FOR_MODEL` / `PLAN_SCHEMA`). The tool is one action: close the recorded address only if that window still exists and still matches the opened entry (never hunt a lookalike), launch the next-ranked stem, bump that stem's weight, and refresh `last_open`. Mutating steps still go through plan → approve → execute. No recent open, a named mismatch (“the other firefox” after Bitwarden), or a single-match query is honest empty-plan chitchat, not a doomed action. Overlay `ACTION_TITLES` gained a friendly label so the plan bubble is readable.
- **Consequences:** Weights persist across overlay close/reopen and brain restarts; `last_open` does not, past 15 minutes, so an hours-later “the other one” will not close a random window. Closing is fail-open: if Hyprland rejects `closewindow` or the address is gone, the alternate still launches. No multi-turn picker UI (out of scope). Covered by `tests/test_jarvis.py` (rank-by-weight, next-stem preview, stale/solo rejection, close+open+bump, dry-run non-mutation, skip-close when the owned window is gone, correction-phrase detection vs intake, planner exclusion, awaiting-approval vs honest empty plan). Not exercised: a live two-Bitwarden Hyprland round-trip in this sandbox — Alex should restart and run feat-open-by-name's new correction steps on 0.5.9.

## ADR-037 — Ground self-improvement in enforceable promotion and distinct evidence planes (2026-09-10)
- **Context:** The initial self-improve roadmap correctly separated Runtime, Desk, Forge and Control Plane, but its Wave 0 briefs assumed private-`main` protection was available, treated human validation and candidate evals as one harness, linked a future ledger to prunable local logs, and described the Control Plane as if it already had narrow file boundaries. A-032 checked those assumptions against the tree and live repository settings. GitHub's ruleset and branch-protection APIs both returned HTTP 403 for this private repository, requiring Pro or public visibility. CI exists and is green, but runs only one of five JavaScript suites. Run journals are ignored and bounded; model/prompt/tool identities are absent. Approval, Training writes, validation and `skills_trusted` recipes span mixed Runtime files.
- **Decision:** Keep the north star and four-subsystem model, but dependency-gate implementation. A-027 is blocked until Alex chooses an enforceable host/plan/visibility and a non-bypass Forge actor. Add A-038 for private content-addressed evidence bundles and exact execution fingerprints before ledger/eval work. Keep CI tests, runtime journal anomaly flags, human validation and candidate evals as four distinct evidence planes. Narrow A-026 to a Desk-owned manual ledger, A-029 to preference memory, A-031 to planner-only non-executing trials, and make A-030 protect the actual authority/trusted-skill surface only after A-027+A-028.
- **Consequences:** A-038 is the first implementation prerequisite in the revised self-improve track. A-026/A-028/A-029/A-030/A-031 are explicitly blocked on named predecessors rather than relying on row order. Foundation work may proceed under today's human-reviewed branch workflow while A-027 is externally blocked, but no unattended Forge, merge or deployment is authorized. CODEOWNERS and policy prose are never reported as a hard gate without API-verified repository enforcement. See `docs/audits/chatgpt-plan-vs-codebase-review-2026-09-10.md` for evidence, risks and the revised dependency graph.

## ADR-038 — origin/main is the only place an assignment claim counts (2026-09-10)
- **Context:** A-036 was claimed and marked `in_progress` only on its own feature worktree/branch; `main`'s `QUEUE.md`/`INDEX.md` kept showing `queued` until a desk agent noticed and hand-synced it. `assignment-status.sh` and the paste prompts (`NEW_AGENT`/`CONTINUE`/`PARALLEL`) read whichever branch the current checkout happened to be on, so a second cold agent following those exact instructions could have claimed the same id again — the isolation worktrees give each agent (ADR-024) never protected the shared bookkeeping files themselves, only the product code.
- **Decision:** A claim is not real until its `QUEUE.md`/`INDEX.md` status flip and `SESSION.md` owner/branch/worktree note are committed and pushed straight to `origin/main` — **before** the claiming agent runs `git worktree add` or writes any product code (`START.md`'s "Required isolation" section, all three paste prompts). `scripts/assignment-status.sh` now fetches `origin/main` and reads its `QUEUE.md` as canonical (falling back to the local branch only when offline, with an explicit warning), lists every worktree's own `SESSION.md` "Active goal" for human visibility, and cross-checks every worktree's local `QUEUE.md` against the canonical rows, printing a `MISMATCH` line for any id whose local status disagrees with `origin/main` (an unpushed or stale claim). A claim that outlives its agent is released by editing `origin/main` directly after checking the owning worktree/branch for unpushed work — never silently reclaimed (`START.md#recovering-a-stale-claim`).
- **Consequences:** Coordination cost moves from "trust the branch you happen to be reading" to one small, fast push per claim — a rejected push means someone raced you and you re-check rather than force through. `assignment-status.sh`'s claim hint, in-progress list and area-disjointness check all now come from the same canonical source, so a second CONTINUE/NEW_AGENT/PARALLEL agent sees a claim the instant it lands on `origin/main`, not only after that work merges back. This does not add a lock server or block `parallel-ok`/area work (ADR-034 is unchanged); it only fixes where the claim itself is recorded. Smoke-tested by reproducing the original bug shape (editing a local worktree's `QUEUE.md` without pushing) and confirming the script reports the mismatch, then reverting.
## ADR-039 — Agent slots expose launch settings and local-state limits (2026-09-10)
- **Context:** The Agent monitor opened visible CLI windows, but “Prepare now” and “Queue locally” did not explain their effects, the busy toggle sounded like process detection, in-progress work was easy to miss behind assignment filters, and Codex launch depth was invisible. The installed Codex CLI accepts `-c key=value`; Alex's current config uses `model_reasoning_effort = "medium"`. Official OpenAI model documentation defines `low`, `medium`, `high`, and `xhigh`, so the UI labels `xhigh` as “Ultra (xhigh)” while storing the real value: https://developers.openai.com/api/docs/models/gpt-5.3-codex
- **Decision:** Cursor/Codex slots may persist one validated `reasoning_effort`. The dashboard shows a slot override or the local Codex config default, and `server.py` passes only the server-loaded slot value to `open-agent.py`; client-supplied launch settings are ignored. The override is added only when starting a new Codex window. Focusing an existing window does not and cannot claim to change that session. Claude Code and Human slots explicitly report that this launcher does not control their depth. Delivery choices are “Open visible window now” and “Save in local queue for later,” each with literal effect text. Busy/idle controls say they change local metadata only. A persistent Now working section derives from disk QUEUE plus slot ownership.
- **Consequences:** No prompt is silently submitted and no hidden agent is started. Immediate delivery still requires the human to paste the reviewable handoff into the visible window; queued delivery never auto-sends. The slot registry stays version 1 with an optional backward-compatible field. A-037 may add formal gate/blocked-by explanations later; A-036 only labels blocked queue rows as waiting. `feat-agent-monitor` is reset to unvalidated for Alex's live window check.

## ADR-040 — Smoke vs full test tiers; quiet-by-default output (2026-09-10)
- **Context:** Alex: the suite felt like "a ridiculous number of tests," worried agents waste tokens running/reading it. Auditing `tests/` found 158 Python `unittest` cases (93 in `test_jarvis.py`) plus five `.cjs` overlay suites, but no near-duplicate/copy-paste bloat — most classes already use `subTest` table-driven cases (5 in `test_jarvis.py` alone) per prior passes' own discipline, and every remaining test name maps to a distinct, named regression. Local wall-clock for the full Python suite is ~0.7s; the five `.cjs` suites total ~0.1s because `tests/overlay.test.cjs` already `require()`s the other four (`training`/`validation`/`assignments`/`agents.test.cjs`) and runs them in the same process — CI's single `node tests/overlay.test.cjs` step was already exercising all five, contrary to an earlier review's assumption that only one of five ran. The actual token cost the assignment named — agents re-reading or pasting full `-v` suite output, and running the full suite reflexively on tiny docs edits — is a tooling/process gap, not a coverage-bloat problem.
- **Decision:** No tests were culled — auditing found nothing safe to cut without weakening a named regression category (honesty, open-app, journal, training, validation), and the assignment itself preferred keeping coverage over false token savings. Instead: `scripts/test-smoke.sh` runs a curated critical-path subset — one whole `TestCase` per named category (`JsonPlanTest`, `ApprovalFlowTest`, `OpenByNameTest`, `JournalTest`, `TrainingTest`, `ValidationTest`, ~71 of 158 cases) plus `node tests/overlay.test.cjs` (all five JS suites) — as the default for docs-only or small changes. `scripts/test-full.sh` mirrors CI (`doctor.sh --syntax`, full `unittest discover`, `shellcheck`, `node --check`, the same `.cjs` entrypoint) for before-merge-to-main or brain/overlay/actions changes. Both write full output to a timestamped file under `logs/tests/` (gitignored) and print only a one-line-per-step summary; on failure each prints just that step's last 30 log lines rather than the whole run. `START.md`'s "Token & context discipline" and all three paste prompts' "Land on main" steps now name this policy explicitly: smoke by default, full before landing/on brain-overlay-actions changes, and never paste `-v`/full suite output into agent context — quote only the printed `FAIL:`/tail lines.
- **Consequences:** `.github/workflows/ci.yml` is untouched — its own `-v` output is for a human reading the Actions log, not for an agent's context window, so the verbosity tradeoff there is different and out of scope. The corrected "all five `.cjs` suites already run in CI" finding replaces the stale claim in ADR-037's evidence (`docs/audits/chatgpt-plan-vs-codebase-review-2026-09-10.md`) that only one of five ran; no CI change was needed to fix it because there was nothing broken. Smoke intentionally omits `test_agents.py`, `test_assignments.py`, `test_workflows.py`, and `test_training_window.py` (Training/agent-monitor plumbing, not the five named critical systems) — an agent touching those areas should reach for `test-full.sh`. Verified: both scripts pass cleanly; a failure was reproduced by injecting a false assertion into `test_journal.py`, confirming `test-smoke.sh` reports it with a bounded tail and correct non-zero exit, then reverted.

## ADR-041 — Structured `Blocked-by` + `Gate` replace stage integers (2026-09-10)
- **Context:** Alex proposed a "stages/waves" idea for release gating. A-032's review had already reshaped the roadmap into informal **Wave 0 / Wave 1** prose (`QUEUE.md`), and the assignment template already carried a free-text `Blocks / blocked-by:` field that no code ever parsed — dependency ordering was entirely human-read prose (e.g. A-030: "After **A-027 + A-028**"), so `assignment-status.sh`'s claim hint offered any `queued` row regardless of whether its real dependency was done, and Training's Agent monitor board (A-036) could only say a blocked row was "waiting — see assignment brief" with no reason. A global integer `stage: 0/1/2` with "shift everything +1" would have duplicated QUEUE order, `parallel-ok`/area rules, the Improvement Ledger's `IMP-*` lifecycle (A-026, a different kind of state), and `docs/MILESTONES.md`.
- **Decision:** Keep one queue (`A-###`, QUEUE order). Split the old prose field into two structured, optional metadata lines parsed the same way `Area`/`Priority`/`Allowed paths` already are (`brain/training.py`'s `META_FIELDS`): **`Blocked-by:`** — `none` or a comma-separated list of `A-###` ids, the only field anything computes claimability from — and **`Gate:`** — an optional lowercase-hyphen grouping label (`control-plane`, `latency`, `training-dispatch`, `plan-review`), purely informational and never auto-enforced. `scripts/assignment-status.sh` now reads every active brief's `Blocked-by`/`Gate` (from `origin/main`, matching A-039's canonical-claim-source rule) and computes each id's *unmet* blockers by cross-referencing **`INDEX.md`** (not `QUEUE.md`) for status — `INDEX.md` retains every id ever filed including `done`, while `QUEUE.md`'s table drops `done` rows entirely, so a `QUEUE`-only lookup would have wrongly treated a completed dependency as still unmet forever. The claim-hint logic now skips any `queued` row with an unmet `Blocked-by`, printing why. Training's `assignments()` (`brain/training.py`) exposes `blocked_by`/`gate`/`unmet_blocked_by` per assignment; the Agent monitor board and Assignments list (`overlay/agents.js`, `overlay/assignments.js`) show the real unmet ids and gate instead of a generic "see brief" hint, and the assignment editor gained matching `Blocked by` / `Gate label` fields validated the same way as the existing optional soft-hint fields (ADR-034's precedent: informational, never a hard gate on their own — `Status` is still a human/desk decision, never auto-flipped by dependency resolution). All ten currently blocked/queued Wave 0/1 briefs (A-026, A-027 external-only, A-028, A-029, A-030, A-031, A-033, A-034, A-035, A-038) were migrated from prose to the structured fields; `A-027` (blocked on Alex's hosting/plan choice, not an id) correctly has `Blocked-by: none`.
- **Consequences:** No new QUEUE.md columns (avoids further table churn on top of A-036's `depth` column); dependency state lives once, on the blocked assignment's own brief. A typo'd or not-yet-filed id in `Blocked-by` is conservatively treated as unmet (absent from `INDEX.md` ⇒ not done) rather than silently ignored. `Gate` intentionally does nothing on its own — grouping/filtering by gate in Training is a UI nicety left for a future assignment if wanted, not built here (out of scope: global stage shift-up, auto-start-next-gate, replacing QUEUE with the ledger). Covered by `tests/test_assignments.py::test_blocked_by_and_gate_are_structured_optional_hints` (format validation, round-trip through the editor, and `unmet_blocked_by` correctly narrowing once a blocker's `QUEUE` row is replaced with a `done` one) and a manual reproduction of the `INDEX`-vs-`QUEUE` bug (a `Blocked-by` referencing a `done`-and-delisted id false-flagged as unmet until the lookup was switched from `QUEUE.md` to `INDEX.md`), verified fixed and reverted before commit.

## ADR-042 — Content-addressed evidence bundles v0 (2026-09-10)
- **Context:** `logs/journal/CURRENT.jsonl` (ADR-017/018) is deliberately ephemeral and bounded — pruned per A-006's retention caps, rotated on every `VERSION` bump — good for "what happened today," useless as a stable comparison baseline across code/model revisions. A-032's review found journal records carried `jarvis_version`/`git_describe` but nothing identifying the exact model digest, planner routing path, system prompt, or tool/plan schema in force for a given run — exactly the inputs the future Improvement Ledger (A-026) and candidate evals (A-028/A-031) need to tell "did the input change" from "did the behavior change under the same input." A-038 is Wave 0's first foundation for that reason: A-026 and A-028 are both `Blocked-by: A-038`.
- **Decision:** `brain/evidence.py` is a small, generic, network-optional module — no dependency on `brain/server.py` beyond what a caller passes in — with four primitives: `read_run_phases()` (bounded, symlink-safe read of an already-written `logs/runs/<run_id>.log`), `fingerprint()` (the exact code/model/planner inputs: `jarvis_version`, `git_revision` — reusing `git describe --always --dirty`'s existing dirty-suffix convention rather than inventing a second one — `planner_mode`, `model`, `model_digest`, `system_prompt_hash`, `schema_hash`, `options`, optional `case_id`/`case_version` for future eval integration), `build_envelope()` (assembles one of three tiers — `reference`: fingerprint + which phases exist, nothing else; `summary` (default): redacted, bounded status/eval/actions/step-count; `full` (opt-in only): adds the raw prompt/reply, still passed through `journal.clean`'s existing secret redaction), and `write_bundle()` (mode 0600/0700, atomic `os.open(O_EXCL)` + `os.replace`, a hard 64KB size cap, symlink-refusing on both the destination and — via `read_run_phases` — the source). The bundle's filename and `id` field are the sha256 of its own canonical JSON, so re-exporting identical content is a verified no-op ("Already durable") rather than a duplicate or silent overwrite — the "collision handling" the assignment asked for. `scripts/export-evidence.py <run_id> [--tier] [--out]` is the bounded callable entry point a human or agent actually runs; it is the only piece that imports `brain/server.py` (for `MODEL`/`VERSION`/`REVISION`/`PLAN_SCHEMA`/`TOOLS_FOR_MODEL`), keeping `evidence.py` itself reusable and independently testable. `brain/server.py`'s request handler now records `mode` (`json_plan`/`tools_run`/`correction`/`intake`/`fixed_plan`) and `model` on each run's `prompt`-phase journal record — the one piece of the fingerprint that wasn't already derivable from existing journal fields — via a minimal reordering (the routing decision that already existed is now made before the `journal_event` call instead of after, with `model` from the already-known `MODEL` global) that keeps every existing behavior identical except that a request that raises *during* prompt/intake/correction routing (regex matches on already-validated input; no observed failure mode) would skip journaling `prompt`, an accepted, documented trade-off for a v0 foundation.
- **Consequences:** Files under `$XDG_STATE_HOME/jarvis/evidence/` (default `~/.local/state/jarvis/evidence/`, gitignored) are durable identities, not a backup — `docs/evidence/README.md` says so explicitly and repeatedly; "backed up" becomes true only once Alex selects and verifies an actual destination. A-026 and A-028 are unblocked (`Blocked-by: none`, `Status: queued`) since A-038 was their only listed blocker; A-030/A-031 stay blocked on their own remaining dependencies. Nothing here implements the ledger itself, a candidate-eval runner, or continuous telemetry (explicitly out of scope) — v0 only defines the shape and the exporter, which A-026/A-028 are expected to reference by content-addressed `id`. Covered by `tests/test_evidence.py` (20 cases: stable fingerprints/content-ids for identical inputs, a mutated model digest addressing a different bundle, dirty-revision pass-through, redaction in both `summary` and `full` tiers, atomicity via a mocked `os.fdopen` failure leaving no partial/temp file, the 64KB bound rejecting an oversized `full` export, malformed/partial-phase sources not crashing, symlinked source/destination refusal, mode 0600/0700, and Ollama-unreachable never raising); manually verified end-to-end against a synthetic run-log and the real local Ollama daemon (`qwen2.5:3b`'s live digest resolved correctly), including idempotent re-export and distinct tiers addressing distinct files. Existing suite: 180/180 pass (160 prior + 20 new); `./scripts/doctor.sh --syntax` and `./scripts/test-full.sh` green.
## ADR-043 — Per-agent auto-queue and auto-advance policy (pending A-041 implementation)
- **Context:** TBD (A-041 redesigns Agent Monitor with per-agent queues and auto-advance when work completes; today's "Save in local queue for later" only stashes a paste-ready handoff and never auto-runs).
- **Decision:** TBD at implement time — whether auto-advance auto-submits into a visible window or requires human paste; must preserve no-hidden-agent / no-silent-spend invariants (ADR-032/036/039). Prefer auto-open/focus + auto-prepare; paste/submit automation only if already supported safely for that backend.
- **Consequences:** TBD — A-041 owns the redesign; this stub only reserves the ADR number and the open policy question.

## ADR-044 — Improvement Ledger v0: Desk-owned, manual, schema-first (2026-09-10)
- **Context:** `docs/assignments/QUEUE.md` is deliberately disposable — an assignment is claimed, done, archived to `done/`, and its bookkeeping row eventually drops out of the open table entirely (A-037/ADR-041 already had to fix a lookup bug caused by exactly this). Nothing durable ties several related assignments (or a rejected idea that never became one) to the single "why did we do this" narrative a human wants months later; `PROGRESS.md` is a chronological session log, not indexed by improvement. A-032's roadmap review named this gap as the Improvement Ledger, deliberately sequenced after A-038 (evidence identity) so a ledger record could reference durable evidence rather than an ephemeral, prunable journal path.
- **Decision:** `docs/ledger/` (`README.md`, `TEMPLATE.md`, `INDEX.md`, `records/IMP-NNN-slug.md`) is manual and schema-first — only Desk (human or desk-role agent) creates or mutates a record; Runtime, Training, and Forge never allocate an `IMP-*` id or write to this directory in v0. Each record has a **current summary** (kept up to date, the only thing a reader needs first) and **append-only events** (a past line is never edited or deleted, even when superseded — this is what "preserve rejection/abandonment history" means in practice). The state vocabulary (`hypothesis → in_progress → shipped | rejected | abandoned`) is informal, matching how an assignment's own `Status:` is already just a human-set label, not an enforced machine. `Evidence refs:` must be either `none`, one or more [A-038](evidence/README.md) content-addressed bundle ids, or a bounded inline summary in Events — an ignored `logs/...` path is never accepted as durable evidence on its own. Id allocation reuses A-039's exact discipline (ADR-038): `git fetch origin`, run `scripts/ledger-status.py` (which validates `INDEX.md` against `records/*.md` — duplicate ids, missing files, a record's own `# IMP-NNN` header mismatching its filename, an `Assignment ids:` entry absent from `docs/assignments/INDEX.md` — and prints the next id computed from `origin/main`, never a branch-local `max+1`), write, commit, push immediately. An assignment may set the optional `- **Improvement:** IMP-NNN` metadata field (added to `brain/training.py`'s `META_FIELDS`/`FIELD_LIMITS` exactly like A-037's `Blocked-by`/`Gate`, same "shape-checked, not existence-checked" validation) to link back; this is a different relationship from `Gate` (which sequences the queue) and changing either never auto-flips `Status`. Seeded six representative records (`IMP-001`…`IMP-006`) from A-019–A-025 and issues #15/#16, each using the bounded-inline-summary evidence path since they predate A-038.
- **Consequences:** A-029 (`Blocked-by: A-026` only) is unblocked (`Blocked-by: none`, `Status: queued`). No Training UI, no runtime/issue auto-linking, no enforced state machine, and no replacement of `QUEUE.md`/assignments — all explicitly out of scope for v0; that integration is a later assignment. `docs/ledger/README.md` and `docs/assignments/README.md` cross-reference each other so a reader lands on the right doc for "what's next" (QUEUE) versus "why did this happen" (ledger). Covered by `tests/test_ledger.py` (9 cases: consistent ledger, empty-ledger next-id, duplicate id, missing record file, header/filename mismatch, unknown assignment reference, orphan record file, unknown status, no-assignment-ids-allowed) and `tests/test_assignments.py::test_improvement_link_is_a_structured_optional_hint` (format validation, and the link surviving an unrelated field edit). `python3 scripts/ledger-status.py` run manually against the real seeded ledger confirms it validates clean and reports `IMP-007` as the next id; the validator's problem-detection paths (duplicate/missing/orphan/unknown-status) were each manually reproduced and reverted before committing. Full suite: 190/190 pass (180 prior + 10 new); `./scripts/doctor.sh --syntax`, `node tests/overlay.test.cjs` and `./scripts/test-full.sh` green.

## ADR-045 — Deterministic candidate-eval foundation: four planes, one exact-oracle case shape (2026-09-10)
- **Context:** A-032's review found no clean separation between CI/unit tests, the runtime journal's per-run `evaluate()`, human validation (`docs/validation/`), and a future candidate-eval mechanism — risking one plane's result being read as proof for another (a journal `flag: null` treated as a passing eval, or a Training Verify silently promoting an exploratory capability). A-038 (evidence identity) was deliberately sequenced first so a candidate-eval result could reference a durable, content-addressed fingerprint instead of an ephemeral journal path; A-028 is that next foundation. Both `A-030` (protect control-plane paths) and `A-031` (stochastic planner evals) are blocked on this — A-030 still also waits on the externally-blocked `A-027`.
- **Decision:** `docs/evals/README.md` documents the four evidence planes as a table (CI/unit, runtime anomaly journal, human validation, candidate evals) and states explicitly that none auto-graduates into another. `docs/evals/schema.json` + `docs/evals/cases.json` define one case shape for v0: `oracle_type: unit-test-reference` only — the oracle *is* an existing `tests/` function, so no new execution machinery is needed and every case is deterministic and side-effect-free by construction. Each case's `kind` (`capability`|`regression`) and `protection_class` (`candidate-capability`|`protected-regression`) are independent axes; `owner: control-plane` on every case means adding or promoting one is a reviewed Control Plane change, not something Runtime/Training does unattended, and a human Training Verify never auto-graduates a case's `protection_class`. `scripts/eval-status.py` validates the schema and confirms every `reference` resolves to a real test class/method via `ast.parse` — never importing or executing the referenced test. Seeded five cases (`EVAL-001`…`EVAL-005`) from known planner/approval history: the honesty-rewrite guard, the plan→approve→execute invariant, and the `skills_trusted` safety boundary as `protected-regression`; A-024/A-025's shipped ambiguous-app-open and "the other one" correction (linked from the ledger's `IMP-001`) as `candidate-capability`. A case's *result* reuses [A-038](evidence/README.md)'s envelope verbatim (`fingerprint.case_id`/`case_version`, already-existing fields, now actually wired) plus two additions (`counts`, `artifact_hash`) documented for A-031's future runner to produce — no runner is built here. `.github/workflows/ci.yml` gained `scripts/check-test-coverage.py` (fails if a `tests/*.test.cjs` isn't reachable from a CI entrypoint, directly or via `require()`, or a `tests/*.py` file doesn't match `unittest discover`'s `test_*.py` pattern — the "no quiet suite omission" contract) and `scripts/eval-status.py`; the workflow's existing `name: checks` / job id `test` already satisfied "one stable, unique `checks / test` job." `START.md` and `docs/assignments/README.md` now require a regression artifact (a `tests/` case, plus a `docs/evals/` entry when it names something worth pinning) or a documented human/VM-only reason before closing behavior work.
- **Consequences:** `docs/validation/catalog.json` is untouched and explicitly never relabeled an automated/protected suite. A-030 stays blocked (still needs `A-027`, which is externally blocked on Alex's hosting/plan choice) — A-028's completion does not unblock it; only `A-027`+`A-028` together do, per A-028's own brief. `check-test-coverage.py` was verified against real omission shapes (an orphaned `.test.cjs` never required by any entrypoint, a Python file not matching `test_*.py`) and a real bug was found and fixed in its first draft: `reachable_cjs()` closed over the module-level `TESTS` constant instead of the `tests` parameter passed to `check()`, so its own unit tests were silently reading the real repo's `tests/` directory instead of the isolated fixture — caught only because a transitively-required fixture file (`deep.test.cjs`, absent from the real repo) made the mismatch fail loudly. Covered by `tests/test_check_test_coverage.py` (5 cases) and `tests/test_eval_status.py` (9 cases: schema validation, duplicate/malformed ids, unknown fields, a class-only reference, a nonexistent reference, malformed JSON). Full suite: 204/204 pass (190 prior + 14 new: 5 in `tests/test_check_test_coverage.py`, 9 in `tests/test_eval_status.py`); `./scripts/doctor.sh --syntax`, `node tests/overlay.test.cjs`, `check-test-coverage.py`, and `eval-status.py` all green via `./scripts/test-full.sh`.

## ADR-046 — No GitHub branch protection for now (A-027 cancelled) (2026-09-10)
- **Context:** A-027 required API-verified protection on `main` (PR + required checks, non-bypass Forge). On a private Free GitHub repo the ruleset/branch-protection APIs return HTTP 403 unless the repo is public or the account has Pro. Alex decided not to buy Pro and not to make `omarchy-jarvis` public right now.
- **Decision:** Cancel A-027. Do not wait on hosting/plan changes. Do not change visibility/billing without a new explicit Alex decision. Do not present CODEOWNERS, local hooks, or written conventions as a hard promotion gate. Unattended Forge, auto-merge, and auto-deploy remain disabled; continue human-reviewed merges under the existing workflow. A-030 may proceed as an ownership/inventory/CODEOWNERS *map* and process docs, acknowledging enforcement is human-only until a future hosting decision. If Pro or another host with real protection appears later, file a **new** assignment — do not reopen cancelled A-027.
- **Consequences:** Wave 0 no longer has an external hard blocker on A-027. Agents reading QUEUE/START/roadmap must treat branch protection as unavailable, not “blocked pending Alex.” Dependent briefs drop A-027 from `Blocked-by`. Risk accepted: `main` is not API-enforced; trust is process + review, not GitHub rulesets.

## ADR-047 — Durable, provenance-carrying app-open preference records (2026-09-10)
- **Context:** A-025's `~/.config/jarvis/app-preferences.json` (v1) stored ranking influence as a bare `{query: {weights: {stem: int}}}` counter — no record of *why* a weight changed, *when*, or under what authority, and no way to inspect or undo one specific influence without editing the file by hand. A-029 hardens only this proven seam (explicitly not a general memory platform): "make every durable influence inspectable and revocable with provenance and explicit authority," while an agent observation must never move ranking in v0.
- **Decision:** `actions/core.py` moves to a v2 schema: `{"version": 2, "records": {id: {...}}, "last_open": ...}` — `last_open` stays exactly the ephemeral, non-durable interaction state it already was. Each record carries `id`, `type` (`app_open_weight`, the only constrained type in v0), `query`, `stem`, `amount`, `source` (`run_id`, `event`), `created_at`, `authority` (`explicit_correction` | `migrated_v1` | `inferred`), `confidence`, `supersedes`/`revokes` (reserved, nullable — no v0 code path populates them yet), `expiry`, `status` (`active`|`revoked`). **Precedence** is encoded as plain integer comparison so `ranked_apps`' existing sort key needs zero changes: `query_weights()` multiplies any explicit-tier (`explicit_correction` or `migrated_v1`) record's amount by `EXPLICIT_TIER_MULTIPLIER` (1,000,000) before summing — one explicit correction always outranks any amount of `inferred` repetition, exactly as required, without `inferred` needing a real write path yet (nothing creates one in v0; the tier ordering is tested with synthetic records). **Migration** is a pure, idempotent, in-memory computation inside `load_app_prefs()` — a v1 file's `{stem:int}` weights become one `migrated_v1` record per (query, stem) with the identical `amount`, so ranking is byte-for-byte unchanged before and after migration; nothing is written to disk during a read. The **backup** (`.v1.bak`, written once) and **quarantine** (`.quarantined-<timestamp>` for anything unparseable or an unknown version) both happen inside `save_app_prefs()`, immediately before it would otherwise overwrite that content — never during a read, so a dry-run or pure ranking lookup never mutates the filesystem. **Locking**: a single `_app_prefs_lock()` (`fcntl.flock` on a sibling `.lock` file) wraps every load-mutate-save cycle in the new mutating functions (`add_preference_record`, `revoke_preference`, `restore_preference`, `record_last_open`); `load_app_prefs`/`save_app_prefs` themselves stay lock-free by design, since flock is per-open-file-description, not per-process — a function that locked internally and then called another locking function would deadlock itself. **Bounded growth**: `save_app_prefs` prunes beyond `PREF_RECORD_KEEP` (500), oldest-revoked-first, then oldest-active if still over the cap. `inspect_preferences`/`revoke_preference`/`restore_preference` (idempotent) are the callable inspect/revoke/restore paths; all mutation still goes through the existing plan → approve → execute path at the tool-call layer — this is only the storage-and-ranking layer beneath it. `correct_open()`'s call site is the only place `amount=1, authority='explicit_correction'` records are created in v0 (no `run_id` is threaded through the `correct_app_open` CLI action, so `source.run_id` is honestly `None` rather than fabricated).
- **Consequences:** No user-visible behavior changes — ranking is provably identical pre/post migration, and the "the other one" correction flow is unchanged from Alex's perspective — so no `docs/validation/` entry is required; `inspect_preferences`/`revoke_preference`/`restore_preference` are backend-only in v0, not yet wired to any chat command or Training panel (a later assignment's job, out of scope here). Linked to the ledger's [IMP-001](ledger/records/IMP-001-bitwarden-ambiguous-app-open.md) (A-029 is a direct hardening of A-024/A-025's shipped capability, not a new improvement) via a new append-only event and the assignment's own `Improvement: IMP-001` metadata. Found and fixed a real bug before committing: `_prune_records`'s `keep` parameter had a def-time-bound default (`keep=PREF_RECORD_KEEP`), so `unittest.mock.patch.object(core, 'PREF_RECORD_KEEP', ...)` in a test silently had no effect — caught only because the pruning test asserted an exact post-prune count. Fixed by having `save_app_prefs` pass the current module-level value explicitly at call time. Covered by `tests/test_app_preferences.py` (19 cases: exact-ranking migration, idempotent migration, one-time backup, quarantine of unknown-version and corrupt files, malformed individual records dropped without being fatal, explicit-outranks-inferred-by-repetition, migrated_v1+explicit_correction combining in the same tier, expiry, revoke/restore round-trip and idempotence, unknown-record/unknown-authority rejection, inspect filtering/ordering, bounded-growth pruning order, and a real 16-thread/40-write concurrency test proving zero lost updates) plus the existing `tests/test_jarvis.py::OpenByNameTest` suite updated to exercise the real v2 API instead of a hand-built v1 dict. Full suite: 223/223 pass (204 prior + 19 new); `./scripts/test-full.sh` green.

## ADR-048 — Claimability is computed; `parallel-ok: YES` is the default (2026-09-10)
- **Context:** A second agent following `prompts/PARALLEL.txt` exactly reported "nothing safely claimable" while A-028 (`area:docs`) held the only `in_progress` row at `638c144` — even though A-029 (`area:actions`), A-033 (`area:brain`) and A-041 (`area:overlay`) were all `queued` with `Blocked-by: none` in three areas nobody was touching. Every open row was `parallel-ok: NO`, so the parallel path was dead in practice. Six forces produced that drift and none pushed back: (1) `brain/training.py::prepare_assignment_save` hardcodes `- **parallel-ok:** NO` into every Training-authored brief *and* its QUEUE/INDEX row, while `parallel-ok` is deliberately outside `META_FIELDS`/`assignment_fields` and every later edit preserves `current['parallel']` (ADR-031) — a one-way ratchet only a manual two-file edit can undo; (2) `docs/assignments/README.md`'s only guidance was "Mark `parallel-ok: NO` for `area:brain` / control-plane by default" with no matching "otherwise YES", and `TEMPLATE.md` lists `NO | YES` with NO in the default position; (3) the field asks a **pairing** question (safe against *what*?) at **filing** time, before anyone knows what will be in progress, so NO is the only defensible answer; (4) it silently became a second, unparsed dependency field — A-040 read `NO while A-039 (docs) is in_progress`, A-025 `NO (follows A-024)`, A-007 `NO (serialize before training-mode UI…)` — all ordering statements that have belonged to `Blocked-by:` since ADR-041, except `Blocked-by` is recomputed live and a stale `NO` is forever; (5) it is redundant with area disjointness (ADR-034), which is computed fresh from canonical `origin/main`, and an AND of a fresh correct check with a stale conservative one always yields the stale one; (6) `area:docs` is a catch-all (product docs + eval specs + CI + prompts + ADRs + the queue itself), so Wave 0 is nearly all one area and docs rows over-serialize each other, teaching filers the wrong lesson. Separately, re-reading ADR-034's own evidence shows what it left unfixed: the A-020/A-021 merge pain was entirely in `PROGRESS.md`/`SESSION.md`/`QUEUE.md`/`INDEX.md` — files every assignment touches regardless of area — not in the product code the area rule protects. Two later incidents confirm that is a bookkeeping-protocol gap, not a parallelism gap: A-026 had to renumber its ADR 043→044 because A-041's stub had taken 043 (`ffde097`), and PR #18 hit conflicts on the same shared docs with *no* parallel agent at all, purely from a stale branch base. Full evidence, option tradeoffs and the claim tables: [`docs/audits/parallel-claimability-2026-09-10.md`](audits/parallel-claimability-2026-09-10.md).
- **Decision:** Claimability is **computed, not declared**. A row is claimable when `Status: queued`, every `Blocked-by` id is `done` in `INDEX.md` (ADR-041), and its `area:` differs from every `in_progress` row's area (ADR-034) — all read from `origin/main` only (ADR-038). The absence of a positive flag no longer refuses a claim. `parallel-ok`'s default inverts to **YES**, and `NO` narrows to a *reasoned* kill-switch describing a property of the row itself, from a closed vocabulary: **`control-plane`** (redefines authorization, promotion, evaluation, trust, or the claim rules — A-030, A-031, A-042), **`single-writer`** (redesigns a shared runtime seam no concurrent writer can tolerate even from another area: `brain/server.py`'s approve/execute or planner routing, the journal writer), **`human-serial`** (Alex asked for it to run alone). A bare `NO` with no reason is a filing bug: tooling warns, and the desk treats it as `YES` pending review rather than honoring it indefinitely. `NO` stays *self*-restricting — it says when this row may be claimed, never that an in-progress row locks the repository (making it mutually exclusive was considered and rejected: A-028 was `NO` while in progress, and mutual exclusion would have blocked the very rows this ADR frees). The claim algorithm's **shape is unchanged**, deliberately, so `scripts/assignment-status.sh` and all three paste prompts keep producing correct answers the moment the flags are corrected, with no code on the critical path: `for row in queue order: skip unless queued; skip if any Blocked-by unmet; if nothing in_progress → take it; skip if parallel-ok is NO; skip if area matches any in_progress area; else claimable`. Alongside it, a **shared-bookkeeping protocol** answers the fear most `NO`s were really expressing: reserve your ADR number in the same claim commit that flips your row to `in_progress`; touch only your own QUEUE/INDEX row and never reorder rows or rewrite the prose block under the table mid-flight; write only your own SESSION lines; append PROGRESS as a new dated section at the end and never edit an existing one; rebase on `origin/main` immediately before merging. Applied: A-029, A-033, A-034, A-035 and A-041 flipped to `YES`; A-030 and A-031 keep `NO` with their control-plane reason written in.
- **Branch protection is orthogonal (see ADR-049).** Claimability does not wait on A-027. `control-plane` as a `NO` reason still means *single-writer-by-review* for that row — the live `main` ruleset gates merges, it does not schedule parallel claims. Unattended Forge stays off ([FUTURE.md](FUTURE.md)). A-030 keeps `NO (control-plane)` while producing the authority/ownership map.
- **Consequences:** Relief is real and was verified twice against the **unmodified** `assignment-status.sh`, by pushing this branch as `main` into a throwaway bare origin and cloning it. At `638c144` (A-028 `in_progress`, `area:docs`): three claimable rows — A-029, A-033, A-041 — where stock `main` printed "No assignment in queue is possible right now." After merging the A-028/A-027/A-029 changes from `main` (A-029 now `in_progress`, `area:actions`): A-033 and A-041 claimable, A-034/A-035 correctly skipped on unmet `Blocked-by`, A-030 and A-042 correctly withheld as reasoned `NO`. Preserved unchanged: worktree+branch isolation (ADR-024), claim-on-`origin/main`-before-worktree (ADR-038), `Blocked-by`/`Gate` (ADR-041), soft path hints never gating a claim (ADR-034), and no silent cloud spend. One risk the area rule still cannot see is named rather than papered over: `brain/server.py` and `brain/training.py` are reached into from several areas (A-029 `area:actions` may touch the former, A-033 `area:brain` instruments it, A-041 `area:overlay` edits the latter, A-042 `area:docs` changes two lines in it), so some legal pairs overlap on one file. The response is *not* re-hardening path lists into a gate — ADR-034 settled that and both files are additive-friendly — but making the overlap visible: the claim hint should print a non-blocking `HEADS-UP` when a candidate's soft path hints intersect an in-progress row's, restoring the useful half of path lists (a human noticing) without the half that mis-fired for A-021 (a machine deciding). That, plus `training.py`'s hardcoded `NO`, the stale-`NO` warning, the `scripts/agent-status.py` issue-side rules that still contradict ADR-034 ("parallel-ok requires Allowed/Forbidden paths", "`area:brain` cannot be parallel-ok"), and the paste-prompt rewrites, are filed as **A-042** — until it lands, the tooling still silently accepts a bare `NO`, so this policy is enforced by review. Splitting `area:docs` into `area:docs` + `area:control-plane` was evaluated and deferred: it is strictly more precise than any flag and would make "single writer for the control plane" fall out of the area check itself, but `AREAS` is a closed tuple in two files and the five area labels exist on the real repository — A-042 carries it as a proposal for Alex, not a unilateral change. An explicit Alex "force parallel" override remains a documented escape hatch, not the policy, because making him the scheduler for every parallel claim is the friction he asked to remove. The ADR-reservation rule earned itself inside one day, in both directions: the number this pass reserved for in-flight A-028 was exactly the one A-028 took (ADR-045), while the A-027 cancellation was written straight onto `main` without reserving and collided with this ADR's original number — hence 047, renumbered from 046 at merge time, the second such renumber in two days. Docs and flags only, no product code: `./scripts/test-full.sh` green apart from one pre-existing race, `tests/test_journal.py::test_write_prunes_journal_archive_on_rotation`, which fails 9 of 10 runs on a fresh clean checkout of `638c144` (4 of 10 here) — `journal.prune` sorts by `p.stat().st_mtime`, a float with roughly microsecond usable precision at current epoch values, so two archives written closer together than that tie and the newest can be pruned instead of kept. Recorded in the audit with `st_mtime_ns` as the likely whole fix; left for its own `area:brain` row.

## ADR-049 — GitHub `main` ruleset enabled (public repo; A-027 outcome revisited) (2026-09-10)
- **Context:** ADR-046 cancelled A-027 because private Free GitHub could not enable branch protection. Alex later made `omarchy-jarvis` **public** and created an active repository ruleset on the default branch (`main`).
- **Decision:** Treat branch protection as **real and in force** for this remote. Verified via API (`GET /repos/Avdbergnmf/omarchy-jarvis/rulesets/22779389`): enforcement `active`; rules include `pull_request` (required approving review count 0), `required_status_checks` with context **`test`** (GitHub Actions), `non_fast_forward` (no force-push), and `deletion` blocked; `bypass_actors` empty. Supersedes ADR-046's "no protection / do not wait on A-027" guidance for agents. Do **not** reopen cancelled A-027 as an implementation checklist — the hosting choice is done; remaining promotion docs (PROMOTION.md, Forge actor split, API evidence pack) can be a **new** assignment if wanted. Unattended Forge / auto-merge / auto-deploy stay **off** and parked in [FUTURE.md](FUTURE.md) (credits). Direct pushes to `main` should fail; land via PR with green `test`.
- **Consequences:** Agents and humans must use PRs into `main`. A-030's "no enforceable gate" prose is outdated for GitHub rules — CODEOWNERS remains an ownership map, but merge-to-main is ruleset-gated. Desk commits that previously pushed straight to `main` need a PR (or an explicit bypass actor, which we are not adding). Update START/QUEUE/roadmap accordingly.

## ADR-050 — Versioned Control Plane boundary and ownership map (2026-09-10)
- **Context:** A-030 needed an honest boundary for authorization, success definitions, trust, evidence, promotion, deployment and ledger integrity. These responsibilities are currently mixed into Runtime files, so protecting only future eval documents would let a candidate change both behavior and the rule used to accept it.
- **Decision:** `docs/control-plane/boundary-v1.json` is the versioned classification source and `.github/CODEOWNERS` mirrors every classification for Alex. `scripts/check-control-plane.py` fails CI when a tracked file under a broad boundary root is unclassified, a named authority path disappears, a `protected-regression` eval refers to a test source without `protected-oracle-source`, or CODEOWNERS drifts from the manifest. Ordinary feature tests may ride with a feature, but existing protected oracles, authorization, trusted bundled recipes, workflows, ledger integrity, the boundary, and promotion policy require an explicitly scoped Control Plane change. The map is deliberately coarse across `brain/`, `actions/`, and `scripts/`; physical extraction is filed separately as `feat-self-improve-control-plane-extraction`.
- **Consequences:** The live ADR-049 ruleset enforces PR + `test`, no bypass/force-push/deletion, but zero approvals and no code-owner review. CI and PR flow are real gates; CODEOWNERS and Alex review remain policy rather than API-required review. A-027 stays closed and unattended Forge/auto-merge/auto-deploy stay off. Six focused tests cover classification, missing-path, protected-oracle, CODEOWNERS, and malformed-manifest failures.

## ADR-051 — Stochastic planner evals v0 (2026-09-10)
- **Context:** A-028 defined deterministic `EVAL-*` cases whose oracle *is* an existing unit test, plus the result shape A-031 had to fill (`fingerprint.case_id`/`case_version`, `counts`, `artifact_hash`). A-030 put evaluator/oracle/baseline edits on the Control Plane. What was still missing was a way to measure **first-answer planner reliability** on 10–20 critical behaviors without approving or executing desktop actions, without an LLM-as-judge, and without turning a tiny-N score into a promotional claim (A-032's review: pass@k is the wrong product target when the user sees one plan).
- **Decision:** `scripts/stochastic-evals.py` is the runner. Each trial is a **fresh child process** that tripwires `commit_plan` / `execute_plan` / `execute_tools_plan` / `restore_target` / `hypr` / `dispatch` / `subprocess.run` / non-local `urlopen` *before* calling one planner surface, and points `LOGS` at a temp directory it then proves stayed empty. Two surfaces, never averaged: **`planner-json`** calls `brain/server.py::json_plan` (the live JSON planner) for N trials (default 3, development band 3–5, hard cap 20); **`router`** calls the extracted `route_prompt()` (the same function the HTTP handler uses) once, because those regexes are deterministic. 20 cases live in `docs/evals/stochastic/cases.json` (`SEVAL-001`…`SEVAL-020`) with a closed oracle vocabulary evaluated in code — tool names checked against the schemas that actually ship. Results are successes/trials **per case** plus action/output consistency; pass@k is explicitly not reported. Raw model text is private (`--private-out`, gitignored); committed artifacts are redacted hashed summaries plus `kind: eval` A-038 bundles (`brain/evidence.py::build_eval_envelope`) that store counts and per-trial output digests, never replies. `baseline.json` ships `unapproved`; `--gate` refuses to run until Alex fills a human-approved tolerance for every planner case. CI runs `--validate` only (no model, no N-runs). `--backend stub` exists so the runner/oracle/isolation/evidence shape can be proven without Ollama.
- **Consequences:** No plan is approved or executed from this suite; no live desktop mutation; no auto-tuning against the set; no unattended promotion. A-028's `EVAL-*` cases stay the deterministic CI gate. Adding, editing or promoting a `SEVAL-*` case is a Control Plane change. Covered by `tests/test_stochastic_evals.py` (registry, oracles, tripwires, stub isolation, unapproved-baseline refusal, eval envelope) and `tests/test_jarvis.py::test_route_prompt_is_the_live_planner_router`. Router trials (6/6) and stub planner trials run in this assignment; live Ollama planner N-runs were not spent from this cloud agent.

## ADR-052 — Latency profiler foundation (reserved by A-033, in progress)
- **Context:** TBD — A-033 adds InteractionTrace / spans / a local store and instruments submit → first meaningful visible content, without a Training UI.
- **Decision:** TBD at implement time.
- **Consequences:** TBD. This stub reserves the number per ADR-048's shared-bookkeeping protocol.
