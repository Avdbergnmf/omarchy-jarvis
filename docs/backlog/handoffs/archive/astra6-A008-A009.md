# Handoff — Astra 6 → A-008 then A-009 (Training mode)

Paste this whole file as your first prompt. Cold-start OK.

## Who / where
- **Agent:** Astra 6 (difficult UX — Training mode + human validation)
- **Repo:** https://github.com/Avdbergnmf/omarchy-jarvis (private)
- **Canonical clone:** `~/Work/omarchy-jarvis` on Omarchy host `try-omarchy`
- **Do not** use a stale `~/Projects/omarchy-jarvis` tree if it exists

```bash
cd ~/Work/omarchy-jarvis
git fetch origin && git checkout main && git pull origin main
./scripts/assignment-status.sh
```

## When you are allowed to start (critical)
A-008/A-009 are **serial overlay** work (`parallel-ok: NO`).

1. **Do not start** while **A-010** (or any other overlay `in_progress`) is active — check QUEUE + SESSION + `git status`. If another agent has uncommitted overlay/brain edits, **stop** and tell Alex; do not overwrite.
2. Prefer **A-007** (slash autocomplete + command registry) **done** before A-008. If A-007 is still queued when A-010 is done, either:
   - wait for whoever owns A-007, **or**
   - claim A-007 first (one assignment), report, then continue to A-008 if Alex said `batch` / `keep going for training`, **or** Alex told you to own A-007→A-009.
3. Then **A-008**, then **A-009**. Default batch for this handoff: **`batch 2`** covering A-008 then A-009 only (not the whole queue), unless Alex widens it. Still **report after each** assignment with evidence.

Alex intent: you own the **training** track (A-008 + A-009). Others may finish A-010/A-007.

## Read first (in order)
1. `START.md` — fully (assignments, parallel rules, token discipline, one-then-report / batch)
2. `AGENTS.md`
3. `docs/HOST.md`
4. `docs/assignments/README.md` + `docs/assignments/prompts/NEW_AGENT.txt` claim rules
5. `docs/assignments/active/A-008-training-mode.md` — **source of truth for A-008**
6. `docs/assignments/active/A-009-human-validation-tests.md` — **source of truth for A-009**
7. Skim: `docs/DECISIONS.md` (esp. approve/execute, self-improve, journal), `README.md`, `docs/LOGGING.md`
8. Existing patterns: `overlay/*`, `brain/server.py` (runs, intake, feedback), `actions/report_bug`, `actions/prepare_handoff`, `docs/backlog/`, RLHF feedback UI from A-005

## Scaffolding already prepared for you
- `docs/FEATURES.md` — feature catalog stub + schema (seed/extend for A-009)
- `docs/validation/catalog.schema.md` — validation entry shape
- `docs/assignments/agents.example.json` — agent-slot registry shape for A-008 monitor
- Desk authoring rules for generated assignments: `docs/assignments/TEMPLATE.md` + README

## A-008 acceptance (must all work at least via prepare-handoff UX)
See checklist in `A-008-training-mode.md`. Summary:
- `/train` + button; Esc/back to chat
- Extend slash **registry** (from A-007; if missing, add minimal registry as part of A-008)
- Panels: issues/backlog/RLHF-neutrals, version, simple metrics viz
- Problem → comments → **confirm** → write assignment + QUEUE (no silent cloud spend)
- Work-on-assignment → agent picker → paste-ready CONTINUE/NEW_AGENT (or confirm before any real dispatch)
- Agent monitor from QUEUE/SESSION + local agent slots; queue-until-free / send-if-idle / new slot
- Never claim an agent was messaged if only a file was written
- Tests + README human section + ADR + PROGRESS; mark A-008 done in QUEUE

## A-009 acceptance
See `A-009-human-validation-tests.md`. Summary:
- Catalog schema + seed existing features
- Training UI: unvalidated list, guided steps, Verify vs Fail→bug/report intake
- Document that future feature work must add validation entries
- Tests + ADR + PROGRESS; mark A-009 done

## Safety / forbidden
- Do not remove plan→approve→execute
- Do not commit secrets, journals, or `logs/`
- Do not force-push; keep CI/doctor green
- Do not auto-spend Cursor/Claude/Astra credits without explicit confirm UI
- Do not collide with in-progress A-010 file ownership
- Prefer prepare-handoff over fake “I spun up the agent” unless a real connector exists

## Host tips
- Hotkey Super+Shift+J; service `./scripts/start.sh`; doctor `./scripts/doctor.sh`
- `gh` auth as Avdbergnmf; repo private
- Omarchy notifications / webapps — see HOST.md
- No worktree isolation on this machine historically — coordinate via QUEUE statuses

## Done means
QUEUE/INDEX updated, assignment files in `docs/assignments/done/`, PROGRESS entries, handoff moved to `docs/backlog/handoffs/archive/` when both A-008 and A-009 are done.
