# Handoff — issue #{{ISSUE}} → {{AGENT}}

Paste this whole file as your first prompt to {{AGENT}}. It is meant to work **cold**, without any other chat context.

## Clone / work location
```
git clone https://github.com/{{REPO}}.git ~/Work/omarchy-jarvis   # or cd into your existing clone
cd ~/Work/omarchy-jarvis
git checkout -b {{BRANCH}}
```
Canonical path on the implementation host is `~/Work/omarchy-jarvis`. Do not use a stale `~/Projects` checkout if one exists.

## Read first
- `AGENTS.md` — project goals, non-goals, safety rules, how to work in this repo
- `docs/HOST.md` — host facts (Omarchy/Hyprland version, packages, live catalog)
- `docs/DECISIONS.md` — ADR log, especially transparency (ADR-014/015) and anything about this issue's layer
- `docs/backlog/README.md` — triage/difficulty legend and agent routing guide

## Issue
- Title: {{TITLE}}
- URL: {{ISSUE_URL}}
- Labels: {{LABELS}}

{{ISSUE_BODY}}

## Acceptance criteria
{{ACCEPTANCE}}

## Forbidden actions
- Do not remove or bypass the plan → approve → execute control plane (ADR-014/015).
- Do not commit secrets, tokens, or `.env` contents.
- Do not force-push, rewrite shared history, or merge without CI green.
- Do not add model-generated shell execution outside the reviewed `actions/` pattern.
- Do not silently contact or spend credits on other agents.
- Keep `~/Work/omarchy-jarvis` canonical; do not fork the layout.

## When done
- Keep `./scripts/doctor.sh` and tests green.
- Update `docs/PROGRESS.md` and `docs/DECISIONS.md` (ADR) for any non-obvious choice.
- Open a PR against `main` referencing issue #{{ISSUE}}; do not merge without the user's review.

---
Generated by Jarvis `prepare_handoff` on {{DATE}}. This file only prepares a prompt; it does not contact or spend credits on {{AGENT}} itself.
