[
  {
    "type": "function",
    "function": {
      "name": "run_skill",
      "description": "Execute an approved recipe. Use open-planning for planning; scratch-and-mail for moving current window to scratchpad and opening email.",
      "parameters": {
        "type": "object",
        "properties": {
          "skill": {
            "type": "string",
            "enum": [
              "open-planning",
              "scratch-and-mail"
            ]
          }
        },
        "required": [
          "skill"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "catalog_bindings",
      "description": "Query live keybindings by description or chord.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string"
          }
        },
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "run_binding",
      "description": "Execute an exact reviewed live binding description or chord.",
      "parameters": {
        "type": "object",
        "properties": {
          "binding": {
            "type": "string"
          }
        },
        "required": [
          "binding"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "workspace_new",
      "description": "Switch to lowest unused positive numbered workspace.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "workspace_switch",
      "description": "Switch to a numbered workspace.",
      "parameters": {
        "type": "object",
        "properties": {
          "workspace": {
            "type": "integer",
            "minimum": 1,
            "maximum": 9999
          }
        },
        "required": [
          "workspace"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "scratch_toggle",
      "description": "Show or hide the scratchpad.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "scratch_move_here",
      "description": "Move the focused application into scratchpad without following it.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "report_bug",
      "description": "File a GitHub issue for something Jarvis got wrong, with a full context pack. Only used after intake Q&A builds the title/body; never call directly on raw chat text.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": { "type": "string", "maxLength": 200 },
          "body": { "type": "string", "maxLength": 8000 },
          "difficulty": { "type": "string", "enum": ["S", "M", "L"] }
        },
        "required": ["title", "body", "difficulty"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "report_feature",
      "description": "File a GitHub issue and local backlog record for a requested feature, with a full context pack. Only used after intake Q&A builds the title/body; never call directly on raw chat text.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": { "type": "string", "maxLength": 200 },
          "body": { "type": "string", "maxLength": 8000 },
          "difficulty": { "type": "string", "enum": ["S", "M", "L"] }
        },
        "required": ["title", "body", "difficulty"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "list_backlog",
      "description": "List open bug/feature issues and the local backlog index.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "prepare_handoff",
      "description": "Write a paste-ready handoff prompt for another agent to pick up a filed issue. Never sends or spends credits on that agent itself.",
      "parameters": {
        "type": "object",
        "properties": {
          "issue": { "type": "integer", "minimum": 1, "maximum": 999999 },
          "agent": { "type": "string", "enum": ["claude-code", "cursor", "human"] }
        },
        "required": ["issue", "agent"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "open_webapp",
      "description": "Open or focus a supported webapp.",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "enum": [
              "Todoist",
              "Google Calendar",
              "Outlook",
              "WhatsApp",
              "YouTube"
            ]
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "open_app_by_name",
      "description": "Open or focus any other installed desktop app by name (exact or close match against installed .desktop launchers), e.g. Spotify, Discord, a game. Use open_webapp instead for Todoist, Google Calendar, Outlook or WhatsApp specifically.",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 60
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false
      }
    }
  }
]
